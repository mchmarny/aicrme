/**
 * Tailwind v4's Vite plugin scans the module graph automatically and needs no
 * content globs. This file exists for editor tooling (the Tailwind CSS
 * IntelliSense extension expects one) and as the place to add theme
 * customization later; it is wired in via `@config` in src/index.css.
 * @type {import('tailwindcss').Config}
 */
export default {}
