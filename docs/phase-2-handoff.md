# Phase 2 handoff

**Status:** Phases 0, 1, 2a, and 2b-i are complete and merged to `main`. Phase 2b-ii (the ConfigMap-backed run store, restart recovery, and the bus epoch) is complete on this branch (`phase-2b-ii`, not yet merged).
**Spec:** `approach.md`. **Phase 2b-ii plan:** `docs/superpowers/plans/2026-08-17-aicrme-phase-2b-ii.md`. **Phase 2b-ii design:** `docs/superpowers/specs/2026-08-17-aicrme-phase-2b-ii-design.md` — its "Open questions" and "Constraint carried for Phase 3" sections carry inputs 2b-iii needs. **Phase 2a plan:** `docs/superpowers/plans/2026-08-15-aicrme-phase-2a.md`. **Phase 2a design:** `docs/superpowers/specs/2026-08-15-aicrme-phase-2a-design.md`. **Phase 0-1 plan:** `docs/superpowers/plans/2026-08-13-aicrme-phase-0-1.md`.

This document records what the Phase 2a build learned that the code and the plan do not already state. It exists because the working ledger those findings lived in (`.superpowers/sdd/2026-08-15-aicrme-phase-2a/progress.md`) was scratch and is deleted at the end of the run. `docs/phase-2a-task-1-findings.md` was a second scratch document created mid-phase for the same reason this one exists; its durable content is folded in below and the file itself has been deleted — git is the record, not a second markdown file.

---

## What works today

`helm install` on Kind/KWOK gives a console that runs the full **Discover → Recommend → Bundle → Apply** arc: login, live SSE timeline, real cluster snapshot via the AICR agent Job, capability statement and gap list, two questions, resolved and version-pinned recipe, a downloadable bundle, and a confirm-gated Apply that runs `deploy.sh` for real inside the production image. Verified live end to end by two scripts: `test/e2e/discover-recommend.sh` proves Discover through the Apply confirm gate, and **`test/e2e/apply-dryrun.sh` is the proof for Apply itself** — it builds and loads the real container image, drives the whole arc through the HTTP API, clicks through to Apply, and asserts against `deploy.sh --dry-run` running inside that image with the pinned toolchain (helm 3.19.0, kubectl 1.34.1). Both scripts share cluster setup via `test/e2e/lib.sh`.

Validate, Prove, Reset, GitOps export, AKS, and real hardware are Phase 3+ and not built. Real-cluster (non-dry-run) Apply is not validated by anything in 2a — see "Phase 2a's exit criterion" below.

### Demoing it — the non-obvious prerequisite (carried from Phase 0-1)

**A plain KWOK cluster cannot resolve a recipe.** With no worker nodes the snapshot has no derivable `accelerator`, so every intent/platform pair fails AICR's coverage post-condition. The cluster must carry AICR's simulated GPU nodes (`kwok/profiles/eks/p5-h100.yaml` via `kwok/scripts/apply-nodes.sh` in the AICR repo; see `kwok/README.md`). `test/e2e/lib.sh` does this correctly — copy its approach.

Against a simulated-H100 KWOK cluster, `training/kubeflow` resolves to **13 components** (`internal/steps/recommend_test.go` pins the resolvable matrix against real fixtures). Two snapshot fixtures are committed and both matter: `snapshot-kwok.yaml` (no GPUs, pins the degenerate case) and `snapshot-kwok-h100.yaml` (simulated GPUs, pins the demo path, and is what Task 1's probe and Bundle both used). Do not delete either.

### The Task 1 probe, folded in from the deleted findings file

Before any of the applier code existed, Task 1 answered the phase's two load-bearing questions empirically rather than by assumption: does `deploy.sh --dry-run` survive against Kind+KWOK at all, and does its stdout marker grammar match what the plan transcribed. Both held, on the host toolchain available at the time:

- **Bundle:** 13 logical `result.Components`, 61 files on disk, 159,731 bytes reported by `MakeBundle`, **352K measured by `du -sh`**. This is the number `charts/aicrme/values.yaml`'s `workDir.sizeLimit: 1Gi` default is sized generously against — helm's own chart cache, not the bundle, is expected to dominate the emptyDir in practice. Separately, a manual run of `make test-e2e-apply` (2026-08-16) measured the *downloaded* bundle -- the gzipped tarball `GET /api/runs/{id}/bundle` streams -- at **26,851 bytes**, versus the 352K on-disk directory it was tarred from. Not a contradiction (gzip plus the tar format's own overhead reduction on mostly-text YAML easily gets a ~13x ratio); recorded here so nobody later assumes one of the two numbers is a bug.
- **Deploy steps vs. components:** the bundle has 13 logical components but `deploy.sh` numbers **14** steps — `kubeflow-trainer-post` is a `local-helm` post-install step parented to `kubeflow-trainer`, numbered separately. Nothing may assume 13 == 14; Feedback 2 below is why the UI now models these as two distinct, correctly-related numbers rather than picking one.
- **Dry-run outcome on that run:** exit 0, all 14 numbered steps printed `└─ ✓ <name> installed`, marker grammar (`┌─ [N/M] <name>  →  <namespace>`, `└─ ✓ <name> installed`, `✓ Pre-flight checks passed`) matched the plan's regexes byte for byte, verified with `repr()` to rule out terminal artifacts.
- **Tool versions on that run — the fact that later mattered most:** `helm version` reported **v4.2.4**, `kubectl version` reported **v1.36.3** (`kind v0.32.0`, `kindest/node:v1.36.1`, KWOK `v0.8.0`). The shipped image pins **helm 3.19.0 / kubectl 1.34.1** (`Dockerfile`). This gap is what "the dry-run ceiling" below is about — the host probe's green result does not generalize to the production toolchain, and it took a later dedicated finding to discover why.

---

## The dry-run ceiling — the single most important finding of this phase

**`deploy.sh --dry-run` cannot validate this bundle past component 3/14, running the real, shipped toolchain.** This is empirically established, not inferred, and it is load-bearing for how `test/e2e/apply-dryrun.sh` is written. Read this before touching that script or `internal/applier`.

Task 11 built and loaded the actual production image and ran the full arc against it (not a host probe). In-image `helm version` and `kubectl version` were confirmed to match the Dockerfile pin exactly: helm v3.19.0, kubectl v1.34.1. Under that toolchain, component 3/14 (`network-operator`) fails deterministically:

```
no matches for kind "NodeFeatureRule" ... ensure CRDs are installed first
```

`network-operator` renders a `NodeFeatureRule` custom resource. That CRD is registered by `nfd`, component 2/14 — but under `--dry-run` nothing is ever actually installed, so by the time component 3 renders, the CRD `nfd` would have registered on a real install simply does not exist. Both `--dry-run=client` and `--dry-run=server` were tried against the in-image toolchain and produced the **byte-identical error** — this is not a flag choice, it is inherent to dry-running an ordered bundle whose later components depend on earlier components' CRDs.

**State this plainly: this is a ceiling on dry-run as a verification technique for ordered bundles with cross-component CRD dependencies. It is not a product defect, and it is not a bug in `network-operator`, `nfd`, or `deploy.sh`.** On a real install, `deploy.sh` runs its 14 steps in numbered order, `nfd` genuinely installs and its CRD genuinely registers with the API server, and `network-operator` renders successfully against a real cluster with real discovery. helm 3 is, if anything, more correct here than helm 4 — it builds typed objects against live API discovery and fails hard on a CRD that provably does not exist, where helm 4's dry-run tolerated the same gap. Neither toolchain is buggy; they disagree only on how strict a dry-run's discovery check should be.

**The earlier host probe's green result (Task 1, above) was a false green.** It was produced by helm v4.2.4 taking a different code path than the shipped helm 3.19.0 — not a version-number footnote, but a materially different command line. `install-upstream-helm.sh.tmpl` (in the pinned aicr module) branches on the detected helm major: `HELM_MAJOR=$(helm version ...); if >= 4 then FORCE_CONFLICTS_FLAG="--force-conflicts"`. The host probe therefore ran with `--force-conflicts` and server-side apply; production runs helm 3 with neither. Task 1's green dry run never established that the production image's helm 3 renders these 13 charts — only that some helm does. Task 11's in-image run is the first (and only) evidence about the toolchain that actually ships.

`test/e2e/apply-dryrun.sh` therefore does **not** assert `state == done`. That state is not merely unmet under dry-run, it is **unreachable, forever** — a test asserting it can never pass, which trains people to ignore CI exactly as reliably as a test that can never fail. Instead it asserts everything dry-run *can* prove and pins the *known* failure precisely: the confirm gate fired, the bundle downloaded, in-image helm's major matches the Dockerfile pin, at least one component reached `installed`, and the run failed at exactly `network-operator` (recipe position 3/14) with an error containing `no matches for kind "NodeFeatureRule"`. Pinning the known failure this specifically means a *different* failure — a real regression — still breaks the build; the script's own failure messages say as much (e.g. "if this now reaches done, the network-operator/nfd CRD-ordering limitation may have been resolved upstream; a human needs to re-verify and update this pinned assertion, not just let it pass").

**If you are reading this because CI is red on `apply-dryrun.sh` and you are about to change the assertion to make it green: stop.** Read the failure message the script prints — if it names `network-operator` failing on the exact `NodeFeatureRule` error, at position 3/14, that is the known ceiling firing correctly and nothing is wrong. If it names a *different* component, a *different* error, or reaches `done`, that is real signal (a regression or an upstream fix) and needs investigation, not a widened assertion. "Fixing" `apply-dryrun.sh` to assert `state == done` would not be a fix; it would delete the only exit gate 2a has, because state == done cannot happen under dry-run regardless of code correctness.

---

> **SUPERSEDED IN PART, 2026-08-17 — read this before the section below.** The
> dry-run ceiling is real and everything this document says about `--dry-run`
> still holds. What does **not** hold is the inference drawn from it: that
> simulated hardware therefore cannot validate the Apply chain, and that Phase 4
> is a hard dependency for answering "does Apply work end to end".
>
> A real (non-`--dry-run`) install on the same KWOK cluster **completes**. All
> 14 components install, the run reaches `StateDone`, and every Deployment,
> DaemonSet and StatefulSet converges to its desired replica count — in 6m02s,
> reproduced on three consecutive runs before this note was written. The ceiling
> was a limit of `--dry-run` specifically, not of simulation: dry-run validates
> each chart against the cluster as it *is*, never as the previous component
> left it, so it can never get past a CRD an earlier component would register.
> A real install registers them and the chain runs.
>
> `test/e2e/apply-real.sh` is now a CI gate asserting exactly that. Phase 4 is
> still required for the **finale** — real drivers, real GPU scheduling, real
> NCCL throughput, the 0-of-64-to-64-of-64 callback — but it is no longer
> required to know whether the install chain works. Two claims below overstate
> this and are marked inline.

## Phase 2a's exit criterion, stated explicitly

**2a is a dry-run / demo milestone.** It proves: the console builds and downloads a real, version-pinned bundle for a resolved recipe; the confirm-gated Apply step genuinely invokes `deploy.sh` inside the production image with the production toolchain; the pipeline UI correctly renders that run's markers as they stream in; and the run fails, deterministically and for a well-understood reason, at the point dry-run structurally cannot go further.

**2a is explicitly not validated for real-cluster Apply**, and it should not be presented or treated as such. Nothing in this phase runs `deploy.sh` without `--dry-run` against real hardware; nothing exercises real driver installs, real GPU scheduling, or a real multi-minute Apply. Full-chain validation *against real GPU nodes* — real drivers, real scheduling, real throughput — still requires Phase 4's hardware. **Corrected 2026-08-17:** the sentence that used to sit here generalised the dry-run ceiling into "dry-run cannot stand in for real hardware, therefore simulation cannot validate the chain". The first half is true; the second does not follow, and is false. A real install on simulated hardware reaches `StateDone` with every workload converged (`test/e2e/apply-real.sh`). What Phase 4 is a hard dependency for is the finale, not the question "does Apply work".

This closes Feedback 1 below on the "should 2a claim more than a demo milestone" question: no, and now there is a reason stronger than caution to say so.

**What the e2e exit gate does *not* exercise.** Because the run always terminates at 3/14, `apply-dryrun.sh` covers only the `started`/`installed`/`failed` marker statuses and the `KindError` path. The async marker (`kai-scheduler`, the one component installed without `--wait`), `All components installed successfully.`, `StateDone` on Apply, the cockpit's `Done` branch, and `POST /retry` (the engine-level whole-run retry) are unit-covered only — no e2e script has ever driven them. The component-level `retrying` marker (`internal/applier/parse.go`'s `StatusRetrying`, emitted when `deploy.sh` retries a single failing component under its own quadratic backoff) sits in between: it is unit-covered by a hand-authored fixture — Task 1's captured transcript had zero retry markers, so there was nothing real to slice a fixture from — but a manual run of `make test-e2e-apply` (2026-08-16, all findings verified) produced genuine `retrying` markers for real: `network-operator` failed and `deploy.sh` retried it under `--retries 5`, so the observed marker sequence was `failed installed retrying started`. That means the parser's retry path is now validated against real `deploy.sh` output too, not only the synthesized fixture — but this was a one-off manual observation, not something CI asserts on every run, so treat it as evidence the path is real, not as ongoing coverage.

Also record: **all five resolvable `(intent, platform)` pairs are GPU-stack recipes containing both `nfd` and `network-operator`** — verified directly against the simulated-H100 fixture (`training/kubeflow`, `training/slurm`, `training/any`, `inference/dynamo`, `inference/any` all include both component names in their resolved recipe). No alternative demo path sidesteps the dry-run ceiling by choosing a different `(intent, platform)` pair; the CRD-ordering limitation fires identically regardless of which of the five is picked. That was written as the strongest available argument that Phase 4 hardware is a hard dependency for full-chain validation. **Corrected 2026-08-17:** it is a sound argument that no `(intent, platform)` choice sidesteps the *dry-run* ceiling, and nothing more. It says nothing about a real install, which completes on the same cluster. The observation stands; the conclusion drawn from it does not.

**The confirm gate's enforcement boundary.** `test/e2e/apply-dryrun.sh`'s `pending == ["apply"]` assertion (right before the bundle download) proves the confirm gate *fires* — the run genuinely parks with only `apply` pending before Apply runs. It does not prove the gate *cannot be bypassed*: the script only ever sends the two decision calls in the order the console's own UI would, so it never attempts the pre-satisfying call a scripted integration could send instead (answering `intent`/`platform` and `apply` together at the very first gate). What actually enforces the boundary is `engine.Decide` (`internal/engine/engine.go`): it now rejects any decision key not present in `e.current.Pending` with `ErrCodeInvalidRequest`, so a single call carrying `{"intent":...,"platform":...,"apply":"yes"}` at the Recommend gate is refused outright rather than silently pre-satisfying `steps.Apply.Requires()` — proven by `TestDecideRejectsKeysNotCurrentlyPending` (`internal/engine/engine_test.go`), not by the e2e.

---

## USER REVIEW FEEDBACK — four external findings and their disposition

Mid-run, an external reviewer raised four findings against the in-progress build. All four were verified rather than accepted at face value, and each was triaged on "does acting now beat acting later," not by severity alone (Ruling 10 in the deleted ledger). This is their durable record.

### Finding 1 — restart recovery is not fully designed; 2a should stay a dry-run/demo milestone

Accurate at the time it was raised. `approach.md` promises ConfigMap checkpointing; at that point in Phase 2a only `NewMemoryStore` existed. Nothing was built against this finding in 2a — what was missing was the *written* exit criterion, which is now stated explicitly above, backed by the dry-run ceiling.

**Disposition: closed in 2b-ii**, item by item — see "Resolved in 2b-ii" below for the disposition of each item on the list, including the one that was already closed earlier (graceful in-flight Apply termination on SIGTERM, in 2b-i). The reviewer's own required-contract list is more complete than anything previously written down and is carried verbatim below so its resolution can be checked against exactly what was asked for. A restart-recovery implementation must provide:

- Run discovery after restart — the console must be able to find in-flight or interrupted runs from persisted state, not just from memory.
- Fail-closed plus explicit Retry rather than auto-resume on an interrupted Apply — an Apply that was cut off mid-`deploy.sh` must not silently resume; it must land in a state that requires an operator's explicit `Retry`.
- Bundle regeneration — the bundle directory lives in an emptyDir that does not survive a restart, so recovery must be able to reconstruct or re-request it, not assume it is still on disk.
- Persisting component/deploy-step state — an in-memory SSE ring cannot replay across a restart, so the store must carry enough state (not just the terminal Run state) to redraw the pipeline UI faithfully after reconnecting.
- Failed checkpoint writes — a `store.Save` failure must not silently wedge the run (see Ruling 13 below, which fixed exactly this class of bug for the in-memory store's callers, but the ConfigMap store introduces a store that can genuinely fail to write).
- ConfigMap size limits — Kubernetes ConfigMaps cap at ~1MiB; a long-running Apply's accumulated event/state history must be bounded or externalized before it can hit that ceiling.
- Graceful in-flight Apply termination on SIGTERM — at the time this finding was raised, nothing canceled a run at all (see "Resolved in 2b-i" below: this is now closed by `Engine.CancelAndWait`). `internal/applier/exec.go`'s process-group SIGTERM→SIGKILL mechanism (Finding 4 below), built in 2a and proven correct by test, is what `CancelAndWait` reaches.

### Finding 2 — 13/14 needs a real data model, not just careful wording

Accurate, and stronger than the plan's original approach (which had proposed set-difference classification with careful wording alone). The reviewer wanted components and deployment actions modeled as two distinct, related things: generated steps grouped under their parent component, displayed as "N components, M deployment actions" rather than picked as a single ambiguous number.

**Disposition: implemented in Task 9.** `web/src/pipeline.ts`'s `deriveComponents` classifies each marker by genuine set difference against the approved recipe (`const generated = recipeKnown && !recipeSet.has(data.name)` — verified to contain zero `endsWith`/`-post`/`-readiness` suffix-rule shortcuts). Generated actions carry `generated: true` and a `parent` attributed to the most recent real component, render indented/de-emphasized in the Cockpit, and `deploymentActionsTotal` reads `deploy.sh`'s own header total rather than an invented count. `web/src/components/Cockpit.tsx` renders both numbers side by side (`{recipe.componentCount} components`, `{actionTotal} deployment actions`). A real defect was caught and fixed in this area before it shipped: `Cockpit.tsx` passing `run.recipe?.components.map(...) ?? []` made every marker classify as `generated` whenever the recipe hadn't arrived yet or was unknown — the classification was not merely lost in that state, it was inverted. Fixed to distinguish "recipe not yet known" (nothing classified as generated, fail-safe direction) from "recipe known and this name is absent" (genuinely generated); see `web/src/pipeline.test.ts`.

### Finding 3 — the probe did not use the shipped toolchain

Verified, and worse than first stated. This is the finding that produced the dry-run ceiling above.

**Disposition: implemented as a hard exit gate in Task 11.** No separate remediation task was needed — Task 11 already builds and loads the real production image for its e2e, so it runs in-image by construction. What was added was an explicit assertion that in-image helm's major version matches the Dockerfile pin, so this class of drift (probe toolchain silently diverging from shipped toolchain) can never again pass unnoticed. The Dockerfile's own pin was correct throughout; the *probe* was the outlier, so there was nothing to change in production code — only in what the e2e proves.

### Finding 4 — cancellation must kill the process tree

Verified, and was a live bug in already-committed code at the time it was raised: `internal/applier/exec.go`'s `cmd.Cancel` sent `SIGTERM` to `cmd.Process` alone. `deploy.sh` traps `TERM` and exits, but its descendants — `install.sh` → `helm upgrade --install --wait --timeout 10m` → `kubectl` — are separate processes in no process group relationship that were never signaled. `cmd.WaitDelay` then `SIGKILL`s only the already-gone `bash`. Net effect: the console would report a run cancelled while `helm` kept installing and kept mutating the cluster underneath it. Neither Task 5's review nor its two fix rounds had caught this, because every test used a fake `Exec`, and the one real-process test spawned a leaf (`sh -c 'sleep 30'`) with no children to orphan — the test could not observe the bug class it needed to observe.

**Disposition: fixed in Task 5b**, inserted as a new task before Task 7 specifically because it was a live bug, the applier was still fresh, and Task 11 was going to be the first task with real child processes (a cancelled e2e run would otherwise have leaked live `helm` processes into CI). The fix: `cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}` makes the spawned process the leader of its own process group; `cmd.Cancel` sends `SIGTERM` to the negative pid (the whole group) rather than the single process; and an escalation goroutine races a `killGrace` timer against a `reaped` channel to send a group `SIGKILL` if the tree is still alive after grace, rather than relying on `WaitDelay` (which only ever targets the single leader process, not the group). This was proven with a real grandchild process (`sh -c 'sh -c "sleep 30" & wait'`), asserting the grandchild is actually gone after cancellation — the class of test the original code lacked. The SIGKILL escalation branch was itself initially untested (Ruling 11) — `killGrace` was changed from a `const` to a package `var` specifically so a test could shrink it and force the escalation branch to fire in well under a second, and an asymmetric bite-proof (mutating only the SIGKILL line, not the SIGTERM line) confirmed exactly one test — `TestBashExecRunCancelEscalatesToSigkill` — catches that specific regression while the other cancellation tests stay green, proving it covers ground the others structurally cannot.

---

## Constraints Phase 3 inherits

**Validate is deferred, and Phase 3 is Prove only. `ValidateState` silently false-passes on every cluster shape this project can demo on.** Measured 2026-08-18 by two throwaway probes, not inferred. AICR's validator schedules its orchestrator Job Pod with a blanket toleration — `Tolerations: []corev1.Toleration{{Operator: TolerationOpExists}}` in `pkg/validator/v1/job_plan.go`, doc-commented as "the orchestrator Job Pod itself always uses tolerate-all scheduling" — plus only a *soft* anti-GPU-node affinity. KWOK's simulated nodes defend themselves with `kwok.x-k8s.io/node=fake:NoSchedule`, a taint whose whole purpose is keeping real workloads off fake nodes; tolerate-all defeats it. The orchestrator lands on a fake node, KWOK's stage-fast controller fakes `Terminated{ExitCode:0}` **without ever starting the container**, and exit 0 becomes CTRF `"passed"`.

Reproduced twice, on deliberately different cluster shapes: on KWOK alone with **nothing installed**, 14/14 checks reported `passed` in under 15s; on Kind + KWOK GPU nodes with **all 13 components genuinely installed**, 13/14 still false-passed. The one that escaped (`ai-service-metrics`) did so only because its catalog entry carries a `dependencyAffinity` toward a real pod — and it then behaved correctly, failing with a useful message (`[NOT_FOUND] no DCGM_FI_DEV_GPU_UTIL time series in Prometheus after 2m0s`). **When a check actually executes, the output is good.** The defect is purely scheduling.

**This is not a KWOK-only problem, and the "it's just simulation" reflex is wrong here.** The fake GPU nodes are a *prerequisite* of the demo path, not an artifact of it: a cluster with no GPU nodes has no derivable accelerator, so recipe resolution fails outright (see "Demoing it — the non-obvious prerequisite" above). Every cluster this console can run its own arc against therefore triggers the bug. The instinct to file this beside the dry-run ceiling — a limitation blamed on simulation that turned out to be one mechanism's artifact — was checked and is **wrong in this case**; the second probe existed specifically to test that instinct and refuted it.

Three facts Phase 3+ needs if it revisits this:
- **The facade cannot distinguish "never ran" from "passed."** Zero checks reported `StatusOther` in either probe; there is no inconclusive state to key on.
- **A reliable detector exists but sits below the facade:** `imageID == ""` together with `startedAt == finishedAt`. `started` alone is *not* reliable — it resets to `false` on any termination, real or faked.
- **`WithValidationTolerations` / `WithValidationNodeSelector` do not help.** Unlike `CollectSnapshot`'s `AgentConfig.NodeSelector`, they affect only *inner* workloads a check spawns, never the orchestrator itself. There is no facade knob for this.

**Per-component check attribution is refuted, independently of the above.** `approach.md`'s "green checks land next to their components in the pipeline" is not buildable as written: `ctrf.Builder` hardcodes `Suite` to `[]string{phase}`, so real output is literally `"suite": ["deployment"]`, and `Name` is the catalog check's own name (`gpu-operator-version`), not a recipe component. One catalog entry has a `componentRef` at all, used for scheduling affinity and never surfaced on the `TestResult`; several checks (`cluster-autoscaling` → Karpenter, `platform-health`, `expected-resources`) have no 1:1 component in this recipe. Any per-component placement would be a hand-maintained, lossy string-matching heuristic. This one is a source-code fact and does not depend on cluster shape.

Decided 2026-08-18: fixing this belongs upstream in AICR — narrowing the orchestrator's tolerations, adding a required anti-simulated-node affinity, or treating an `imageID == ""` termination as not-executed and reporting CTRF `other`. **No upstream issue was filed and none should be while this repo is private**; the finding stays here until `aicrme` is public. Do not build a Validate screen against the current behavior, and do not work around it inside this console.

**A committed snapshot fixture is usable for `ValidateState` after all.** Probe 1 expected `CollectSnapshot` to be mandatory, because `pkg/client/v1/types.go` says the measurement payload lives in an unexported field "for zero-copy round-trip through `ValidateState`". It is not: `internal/aicrclient/options.go`'s own `decodeSnapshot` already round-trips `yaml.Unmarshal` → `aicr.WrapSnapshot` with no live agent. Useful for any future test that needs a snapshot without paying for an agent Job.

**Exact ownership attribution is deferred, with a stated route.** 2b-iii's correlation is temporal — "cluster activity while `<action>` installs" — and deliberately does not claim a workload belongs to the action installing it (see "Resolved in 2b-iii" below). Exact attribution is achievable and was scoped out on purpose, not for lack of a mechanism: `helm get manifest <release>` returns precisely the resources a release owns, and Pod owner traversal (Pod → ReplicaSet → Deployment) closes the gap for pods; neither needs a change to AICR's own output (`docs/superpowers/specs/2026-08-17-aicrme-phase-2b-iii-design.md` §1, "Exact ownership, deliberately deferred"). It was deferred because temporal correlation is honest and far cheaper, and because this phase had no evidence yet of how misleading temporal correlation actually is on a real install — evidence `test/e2e/apply-real.sh` now produces. Revisit with that evidence in hand, not before.

**The recovered-run screen is still not designed.** 2b-ii's fix made a recovered run *usable* under time pressure (Discard everywhere, Retry in `failed`) but that affordance never had design attention, and 2b-iii's design doc named this explicitly as out of scope — pairing UI design work with the informer lifecycle work would have put two unrelated risk profiles behind one review surface. Nothing in 2b-iii touched `web/src/components/Wizard.tsx` or `App.tsx` (verified: zero diff across the phase). Still open for whoever picks it up next.

**`StateActive` is declared but unreachable.** `engine.execute()` unconditionally finishes at `StateDone`; no `Step`/engine hook lets a Prove step park in the terminal-but-active state the spec's §6 requires. Nothing in 2a, 2b-ii, or 2b-iii revisits this. Phase 3 must add the hook — and when it does, see "Deferred findings → Observer" below for a consequence 2b-iii found waiting on the other side of that hook: a recovered `StateActive` run would start scoped Pod/Event watches for its namespaces with nothing left to ever tear them down until an operator's Discard.

**Reset is never automatic — operator-initiated and operator-confirmed, always.** Stated in `approach.md`'s Reset section on 2026-08-17, because it had only been implied by the words "Teardown button" and was nowhere written down. Apply — strictly less destructive than Reset — already has a confirm gate the console cannot pass without a recorded decision, so Reset carrying a weaker contract would be backwards; that it does not today is an accident of Reset not being built. Nothing may trigger it on the operator's behalf: not a failed run, not a restart, not a timeout, not a recovered run being discarded. A run that ends badly leaves the cluster exactly as it is and says so. **Note this is unrelated to Phase 2b-iii's *informer* teardown**, which is an in-process watch lifecycle touching no cluster state — the word is overloaded in this project and the two came up adjacently.

**Reset is never automatic — operator-initiated and operator-confirmed, always.** Stated in `approach.md`'s Reset section on 2026-08-17, because it had only been implied by the words "Teardown button" and was nowhere written down. Apply — strictly less destructive than Reset — already has a confirm gate the console cannot pass without a recorded decision, so Reset carrying a weaker contract would be backwards; that it does not today is an accident of Reset not being built. Nothing may trigger it on the operator's behalf: not a failed run, not a restart, not a timeout, not a recovered run being discarded. A run that ends badly leaves the cluster exactly as it is and says so. **Note this is unrelated to Phase 2b-iii's *informer* teardown**, which is an in-process watch lifecycle touching no cluster state — the word is overloaded in this project and the two came up adjacently.

**[CLOSED — Phase 5, 2026-08-22] Reset must still route through `finish` before bumping the epoch.** Satisfied, but not the way this note anticipated. `engine.Reset` does not drive the superseded run through `finish` itself; it refuses to act on any run that still has a goroutine driving it. `isLive` now includes `StateResetting`, and Reset rejects every live state up front, so by the time it reaches `e.epoch++` the run's own execute goroutine has ALREADY called `finish` — that call is what set the terminal state Reset requires. The dead epoch-guard early returns in `execute` therefore stay dead through this feature, and no non-terminal record can be left in the ConfigMap by a Reset. One residual nuance, recorded because it is the same shape of gap: Reset replaces `e.done` with its own channel, so a `CancelAndWait` arriving in the window between the superseded goroutine's `finish` and its `close(done)` snapshots the new channel and does not wait on the old one. Harmless today — that goroutine has nothing left to do but close — and worth knowing before anything is added after `finish` in `execute`. Original note follows.

> **Phase 3's Reset must still route through `finish` before bumping the epoch — and with a real store the consequence is now durable, not just in-memory.** `Engine.execute`'s two epoch-guard early returns (`internal/engine/engine.go`, immediately after each `aliveLocked` check) are dead code today — `epoch` only advances via `Retry`, which is gated on `StateFailed`, a state only the same goroutine's own `finish` call sets, so no goroutine can currently observe a supersession mid-flight. Reset is exactly the feature that makes them reachable: if Reset bumps `epoch` without first driving the superseded run through `finish`, the superseded goroutine's next `aliveLocked` check fails, it returns without ever calling `finish`, and `done` closes with no terminal state ever persisted — the same completion-contract gap `CancelAndWait` was built to close (below), reopened by a different caller. Before 2b-ii this bug was purely in-memory, invisible past the next restart. Now that `cmd/aicrme/main.go` wires the ConfigMap store into production, the same mistake leaves a stale non-terminal record sitting in the ConfigMap, which the next startup's `Recover` finds and marks `StateFailed` with `"interrupted by a console restart"` — durably misreporting a run Reset was actually superseding on purpose, until an operator notices and clears it. Reset's implementation must call, or otherwise guarantee, `finish` before it increments `epoch`, not after.

**Ownership evidence must be captured before Apply, and GitOps export inherits the same problem.** Phase 5's finding, recorded here because the next feature to touch what a run "owns" will hit it too. AICR's generated `install.sh` runs `helm upgrade --install` and `--create-namespace`: a release or namespace that already existed at the same name is adopted silently, prints a deploy header like any other action, and lands in `run.Components` indistinguishable from something the run created. Neither leaves a marker, so the cluster cannot be asked afterward what was already there. `internal/steps.snapshotOwnership` therefore runs immediately BEFORE `deploy.sh` — the last moment the answer exists — and a second pass after Apply records the UID of each namespace the run went on to create. A GitOps export that claims to describe "what this run installed" has exactly the same ambiguity to resolve and exactly one place to resolve it: it must read `Run.Ownership`, not `Run.Components`, or it will export someone else's release into a repository as though the console had put it there. `helm list` must carry `--all` for the same reason the snapshot does: a release left failed by an earlier attempt is still one the run did not create.

**The training workload does not exist.** `demos/workloads/training/` in the AICR repo holds only `gke-nccl-test-tcpxo.yaml`. The spec's "Training + Kubeflow → TrainJob NCCL all-reduce" finale has no source material and must be authored in Phase 3. Inference is well covered (`vllm-agg.yaml`, `nimservice-llama-3-2-1b.yaml`, `chat-server.sh`, `chat.html`).

**`aicrclient` has headroom.** `MakeBundle`, `BundleComponents`, and `ValidateState` all exist on `*aicr.Client`; `MakeBundle` is consumed (Phase 2a), `ValidateState` is not yet. The single-method interface decomposition accommodates a `Validator` additively with no restructuring, same as it did for the `Bundler` Phase 2a added.

---

## Resolved in 2a — removed from the inherited deferred list

The following five items, carried forward from the Phase 0-1 handoff as things Phase 2 would inherit, are closed. Do not re-open them without a new finding.

- **`readOnlyRootFilesystem`** is now set (`charts/aicrme/templates/deployment.yaml:46`). Task 3 built the writable emptyDir (`workDir`, `sizeLimit: 1Gi`, sized against Task 1's measured 352K bundle), the six tool-cache env vars pointing helm/kubectl/bash scratch space at subdirectories of it, and `ensureWorkDirs` to create them at startup (an emptyDir mounts empty). Verified: the only production filesystem write outside it is `internal/steps/bundle.go`'s bundle generation, which is inside the emptyDir.
- **The `engine.Start` context / 409 latch.** `handleCreateRun` passing the cancellable request context into `Start`, such that a `store.Save` failure would leave `e.current` live with no goroutine and permanently 409 new run creation, is fixed. `Start` (like the new `Retry`, below) now rolls back `State`/`Err` on a `store.Save` failure, guarded by an identity check (`e.current.ID == r.ID`) and an epoch-aliveness check so the rollback cannot stomp a legitimately newer run that took over in the interim (Ruling 13). Inert today (`memoryStore.Save` never errors) but no longer a landmine for 2b-ii's ConfigMap store, whose whole point is a `Save` that can fail.
- **The epoch guard.** `internal/engine/engine.go` now carries an `epoch uint64` field, bumped on every `Start` and `Retry`, checked via `aliveLocked(epoch)` at six checkpoints: `execute` (twice — on entry and again just before it calls `finish`), `awaitDecisions` (once), `runStep` (twice — on entry and again at the merge-back after the step returns), and `finish` (once). This is what stops a superseded run's stale goroutine from writing state after a `Retry` has launched a new one for the same run ID. It is currently **unreachable by any black-box test** — `Retry` only launches when `State == Failed`, which is set exclusively by the prior goroutine's own terminal `finish()`, so by the time any external caller can observe `Failed` and call `Retry`, the prior goroutine has no shared-state work left to race on. The guard is proven correct by a white-box test in `internal/engine` (package-internal, since `engine_test.go` is `package engine_test` and cannot reach the unexported field) that manufactures supersession directly. This is documented at the code site and here so the next phase does not read "0% naturally reachable" as "delete it" — Reset (Phase 3) is exactly the feature that will make it naturally reachable, and re-adding a concurrency guard correctly under time pressure later is a worse position than carrying an artificially-tested one now.
- **The 401 `authed` reset.** Fixed in Task 10 — `GET /api/session` backs an unmount-safe probe from `EventSource`'s error handler; on a real 401 the console now returns to the login screen instead of sticking on "reconnecting…" forever after the 8-hour session expiry.
- **`Discover.tsx`'s capable-vs-no-snapshot conflation.** Fixed in Task 10 — the component now checks `!report.analyzed` before `gaps.length === 0`, so a payload missing or with a degraded `analyzed` field (including a stale fixture that predates the field) degrades to an explicit caveat rather than a false "already capable" claim. Same fail-safe direction as the recipe-classification fix in Finding 2 above.

---

## Resolved in 2b-i — removed from the inherited deferred list

`make qualify` is green on this branch at aggregate coverage 85.7% (floor 80%, `.settings.yaml`'s `quality.coverage_threshold`). The following three items, carried in this document's own inherited-constraints list at the time (then named "Constraints 2b-ii inherits", formerly "Constraints 2b inherits", renamed "Constraints 2b-iii inherits", and renamed again "Constraints Phase 3 inherits" above now that 2b-iii has shipped too), are closed. Do not re-open them without a new finding.

- **Nothing canceled a run, and shutdown was ungraceful.** Fixed. `Engine.CancelAndWait(ctx) error` (`internal/engine/engine.go`) is the real completion contract this constraint was missing: it cancels the in-flight run's step context and blocks until its `execute` goroutine has exited *and* persisted a terminal state, or `ctx` expires — idempotent, and a no-op with no run in flight. `cmd/aicrme/main.go`'s shutdown path calls it: mutating endpoints drain first (`srv.Drain()`, so an unguarded `POST /api/runs` cannot start a run that shutdown then kills mid-flight), then HTTP shutdown and `eng.CancelAndWait` run concurrently under a `sync.WaitGroup` — not sequentially, because the invariant is "do not return before the `deploy.sh` process tree is reaped," not "finish HTTP first." `internal/applier/exec.go`'s process-group SIGTERM→SIGKILL mechanism — built in 2a, proven correct by test, but unreachable in production (Finding 4's disposition, above) — is reachable now: a pod delete or a SIGTERM on `aicrme` reaches `cmd.Cancel` for real. A late re-review corrected the budget's own rationale: the true worst case is `killGrace` (10s, the SIGTERM→SIGKILL escalation) plus `terminalSaveTimeout` (5s, the engine's detached terminal-state write) — not two `killGrace` windows back to back. `cmd.WaitDelay` does not add a second escalation window: `os/exec`'s `watchCtx` starts the `WaitDelay` timer the instant `cmd.Cancel` returns, the same moment the escalation goroutine starts its own, so the two race concurrently rather than run in sequence (the doc comment on `Cmd.WaitDelay` says the timer starts on whichever of context-done or process-exit occurs first). `runShutdownTimeout` is 30s — real headroom over the ~15s worst case, not the zero-slack margin the earlier rationale implied — and `test/chart/contract.sh` now asserts `runShutdownTimeout >= killGrace + terminalSaveTimeout` directly, reading both constants from source, so the two cannot drift apart silently.
- **The observer's non-existence.** `internal/observer` now exists, is wired into `cmd/aicrme/main.go`, and runs from pod start: shared informers over DaemonSets, Deployments, and Nodes, converting watch events into typed `bus.KindCluster` events scoped to the active run's resolved recipe namespaces. It shipped short of `approach.md`'s design and short of the cockpit's eventual needs at the time this bullet was written — 2b-iii closed both gaps; see "Resolved in 2b-iii" below.
- **The pre-existing `gopkg.in/yaml.v3 v3.0.1 // indirect` mismarking.** Closed as a side effect of Task 5's `go mod tidy` (run to promote `k8s.io/client-go` and friends out of the indirect block for `internal/observer`'s new imports): `yaml.v3`, `k8s.io/api`, `k8s.io/apimachinery`, and `k8s.io/client-go` all moved from the `// indirect` block to the direct `require` block in `go.mod`. No version number changed anywhere in the diff — `github.com/NVIDIA/aicr` stays pinned at `v0.19.0` — and `go.sum` has a zero-line diff, since all four promoted modules' checksums were already recorded as indirect-dependency entries.

---

## Resolved in 2b-ii — removed from the inherited deferred list

`make qualify` is green on this branch at aggregate coverage 88.3% (floor 80%, `.settings.yaml`'s `quality.coverage_threshold`). The following items, carried in this document's own inherited-constraints list at the time (then named "Constraints 2b-ii inherits", renamed "Constraints 2b-iii inherits", and renamed again "Constraints Phase 3 inherits" above) and in Finding 1's required-contract list above, are closed — except where the item below says otherwise. Do not re-open a closed item without a new finding.

**The ConfigMap-backed `engine.Store`.** Implemented (`internal/engine/cmstore.go`, `internal/engine/envelope.go`). One ConfigMap, one key (`binaryData["run"]`); every `Save` overwrites the whole record rather than merging into it, so two interleaved writers cannot tear the state — last write wins, and the object's own resourceVersion still catches genuine external conflicts. The payload is a versioned, gzip-compressed envelope (`envelopeVersion = 1`; `decodeRun` refuses a version it does not recognize rather than partially decoding it, so a record written by a different image is never silently downgraded); `bundle.path` is dropped on encode because it points into an emptyDir that does not survive a restart, and handing a recovered Apply a path to a directory that no longer exists would be strictly worse than the key being absent. Writes carry a bounded conflict-retry loop (5 attempts, absorbing a leftover replica mid-rollout or a human `kubectl edit`) plus an in-process mutex whose only job is keeping this process's own concurrent Saves from burning that retry budget on each other. Every ConfigMap call — Save, Load, Delete — is bounded by a 10s timeout (`withCallTimeout`) that composes with the caller's own deadline rather than replacing it, so a wedged API server cannot hang an HTTP request indefinitely. The ConfigMap's `ownerReference` targets the Deployment — resolved once at startup via `AICRME_DEPLOYMENT_NAME`, which the chart sets from the same `aicrme.fullname` helper that names the Deployment object itself, so the two cannot drift — never the ReplicaSet, which is replaced on every rollout and would otherwise garbage-collect the run state as a side effect of upgrading the console. `cmd/aicrme/main.go` wires it in; the chart sets `strategy: Recreate` so a rollout can never leave two writers live at once (see "New for 2b-ii" below for what `Recreate` costs).

**The bus's `nextID` epoch.** Closed. `bus.Bus.Epoch()` returns an opaque per-process identifier, emitted as a named, ID-less `event: epoch` SSE frame ahead of any replay on every connection (`internal/api/events.go`) — named so `EventSource` routes it to its own listener rather than `onmessage`, and id-less specifically so it cannot itself advance the client's replay cursor. `web/src/useEvents.ts` tracks the epoch across reconnects: the first connection just records it; a later connection where the epoch has changed treats `lastId` as a cursor the new process never issued, discards accumulated events, and reconnects at `since=0` instead of trying to patch the existing connection. Server-side, `internal/bus/bus.go`'s replay additionally treats a `since` above the current `nextID` as an impossible cursor and replays from 0 rather than returning nothing — a second, independent guard for the case a client's own epoch tracking misses.

**Finding 1's restart-recovery contract, enumerated.** The reviewer's required-contract list (above) is carried verbatim; here is its disposition, item by item:

- *Run discovery after restart* — **closed**. `Engine.Recover` runs before `:8080` starts accepting connections, loads via `Store.LoadCurrent` under a short bounded retry (3 attempts, 50ms backoff, for the plausibly-transient failure class only — a decode failure or unsupported version fails on the first attempt), and installs the result as the current run.
- *Fail-closed plus explicit Retry rather than auto-resume* — **closed**. Recover lands any run it finds live (`StateRunning`/`StateAwaitingDecision`) or merely `StateIdle` at `StateFailed`, with `Err` set to `"interrupted by a console restart"`. `Pending` is cleared on every recovered record regardless of state, not only on the ones Recover flips: this document previously claimed the self-inconsistent `state=failed pending=[…]` combination was closed while the clear still sat inside the `isLive||StateIdle` branch, so the one case that most needs it — a SIGTERM at a decision gate, where `finish()` writes `StateFailed` over a run whose `Pending` `awaitDecisions` had just set — was the case it never covered. `Start` refuses with a 409 while `recoveredPending` is set; only `Retry` or `Discard` clears that gate — nothing auto-resumes an interrupted Apply.
- *Bundle regeneration* — **closed**. Recover rewinds `StepIndex` back to the Bundle step for any retryable run whose index is past it, identifying Bundle by `Phase()` rather than a hardcoded index so the rewind target cannot silently rot as steps are added or reordered. `Retry` then re-executes from that rewound index, and Bundle's step body already re-resolves the recipe from persisted inputs rather than trusting anything on disk (see `internal/steps/bundle.go`'s `NewBundle` doc comment), so the bundle directory the restart's emptyDir wipe destroyed gets rebuilt for free instead of a recovery path having to assume it is still there.
- *Persisting component/deploy-step state* — **closed**. `Run.Components []ComponentState` is a bounded projection — one row per component, overwritten in place, not a log — merged back into `e.current` on both the success and the failure path of `runStep` (the failure-path gap was caught in review, Ruling 14), checkpointed with every save, and replayed on recovery as bootstrap `KindComponent` events so a reconnecting SPA redraws the pipeline without a second fetch path.
- *Operator affordances for a recovered run* — **closed late, and it was the phase's one Critical.** The design spec claimed the SPA's "existing replay path renders it with no frontend change." That was true of the *events* and false of the *affordances*, and no task's scope included `web/src/` as a result. `recoveredPending` is cleared only by `Retry` (which requires `StateFailed`) or `Discard` (which had no caller anywhere in the SPA), and the sole Retry button lived inside `Cockpit`, which `Wizard` renders only for `bundle`/`apply`. A restart at the Recommend decision gate — the longest idle window in the product, since it waits on a human — therefore reached a console that rendered "Resolving the recipe for the answers you gave…" forever with no button anywhere, and any `helm upgrade` of a release that had completed one demo recovered a `done` run that `Retry` refuses outright. The fix: recovery publishes a dedicated `bus.KindRecovered` marker (not a `KindPhase` message — `deriveRunState` turns every unrecognized phase message into `running`), `deriveRunState` carries it as `RunState.recovered`, and `Wizard` renders a phase-independent recovery panel keyed on **run state** that offers Discard in every state and Retry only in `failed`. `web/src/api.ts` gained `discardRun`, and `App.tsx` restarts a run on discard so the console recovers without a page reload.
- *Failed checkpoint writes* — **closed as "no longer silent," not as "zero consequence."** `Decide` persists the operator's decision before acknowledging it (a detached 5s save; on failure it rolls back Decisions/Pending/State/UpdatedAt together, closing a partial-rollback gap review caught mid-phase). Mid-step checkpoints (`awaitDecisions`, `runStep`) are deliberately best-effort: a failure there logs at Warn and degrades recovery granularity rather than failing a live run over a checkpoint. The terminal write (`finish`) is different again — visible (logged at Error, plus a `KindError` bus event telling the operator directly) but not retried, so a terminal-save failure is a real, accepted residual: the next restart recovers a stale earlier checkpoint and marks it failed, even if the run actually finished successfully. See "New for 2b-ii" below.
- *ConfigMap size limits* — **closed**, with the caveat the design carried in from the start. `envelope.go`'s `maxPayload` (800 KiB) fails the encode closed with a named error rather than letting the API server reject an oversized object with something opaque; `maxDecompressed` (8 MiB) bounds what a read will ever inflate, so a malformed or hostile record cannot turn into an OOM kill under the pod's 512Mi limit. Whether 800 KiB is generous enough against a real cluster's snapshot is a separate, still-open question — see "New for 2b-ii" below.
- *Graceful in-flight Apply termination on SIGTERM* — already closed in 2b-i (`Engine.CancelAndWait`, see "Resolved in 2b-i" above). Listed here again only because the reviewer's contract named it as one item among seven; no 2b-ii work was needed for it.

**`apply-dryrun.sh`'s amd64 coverage — now verified, and the first CI run exposed two things nobody predicted.** This document previously carried the coverage gap as "an inference, not a verified fact": every prior execution had been on a developer's arm64 Mac, and `.github/workflows/e2e.yaml`'s three jobs had literally never run in CI at all, because every phase so far merged straight to `main` and the workflow triggered on `pull_request` only. All of that is now closed:

- The workflow now triggers on pushes to `main` too (`9a59eb7`), so it actually runs.
- The suite's first-ever CI execution, on `ubuntu-latest`/amd64, ran and passed: `smoke`, `discover-recommend`, and `apply-dryrun` are all green, with the dry-run ceiling landing exactly where this document pins it — `network-operator` failing at recipe position 3/14 on the nfd CRD-ordering limitation (run `32025602194`; `apply-dryrun`'s own pass line reads "fails at network-operator (3/14) on the nfd CRD-ordering limitation, exactly as pinned"). The inference was correct: the dry-run ceiling and its marker grammar are architecture-independent.
- Getting there took two real fixes first, neither an architecture problem, both worth recording because neither was predicted:
  - **A capacity assumption.** AICR's snapshot agent requests 1000m CPU by default. Pinned to the one untainted node in a KWOK cluster (the other six carry `kwok.x-k8s.io/node=fake:NoSchedule`, which the agent deliberately tolerates none of), that request does not fit alongside the console and kube-system on a shared CI runner — `0/7 nodes are available: 1 Insufficient cpu` — and Discover sat `Pending` for its full ten-minute timeout (`5f8656a`). Fixed by plumbing `AICRME_SNAPSHOT_REQUESTS` through `steps.DiscoverConfig` to `aicr.AgentConfig.Requests`, mirroring `AICRME_SNAPSHOT_NODE_SELECTOR` exactly: a test-path knob, deliberately absent from `values.yaml`, because a simulated cluster cannot satisfy an assumption a real one can. Both KWOK e2e scripts set `cpu=200m`.
  - **A `pipefail` + `grep -q` interaction.** `writer | grep -q pattern` is unsound under `set -o pipefail` whenever the writer keeps producing after the match: `grep -q` exits at the first hit and closes the pipe, the writer takes `SIGPIPE`/`EPIPE`, and pipefail reports the *writer's* exit status, not grep's — so a successful assertion can fail the script. Not a timing race, which is why it looked machine-specific: macOS ships `bsdtar`, which tolerates the truncated write and exits 0; GNU tar on the Linux runner reports `tar: stdout: write error` and exits non-zero. It passed on every laptop and failed on the very first CI run (`50ea3ba`). All five instances across the suite were converted to capture-then-match; three had been safe only because their payload happened to fit the 64KiB pipe buffer, which was never a property worth depending on.

---

## Resolved in 2b-iii — removed from the inherited deferred list

`make qualify` is green on this branch at aggregate coverage 89.8% (floor 80%, `.settings.yaml`'s `quality.coverage_threshold`) — observer 79 tests at 93.8%, bus 21 tests at 98.3%, web 121/121. The following items, carried in this document's own "Constraints 2b-iii inherits" list (now renamed "Constraints Phase 3 inherits" above) and in "New for 2b-ii"'s open calibration questions below, are closed. Do not re-open a closed item without a new finding.

**Per-component live sub-status.** `approach.md`'s promised live line — `nvidia-driver-daemonset 3/8 ready` attached to the row that is installing — now renders, but on temporal terms this document's earlier text did not anticipate, not the per-component namespace-ownership join it speculated about. `bus.ClusterData` (Task 1, `internal/bus/cluster.go`) is the typed payload; `engine.Attribution` (Task 2, `internal/engine/attribution.go`) is a cheap, lock-guarded snapshot of the deployment action currently installing, updated by `runStep`'s `emit` closure in `internal/engine/engine.go` strictly *after* the marker reaches the bus, never before (see "New for 2b-iii" below); `RunScope.Component` (Task 3, `internal/observer/observer.go`, composed in `cmd/aicrme/main.go`'s `newObserverScopeFn`) stamps that action onto every `bus.KindCluster` event the observer publishes; and `web/src/pipeline.ts`'s `foldClusterEvent`/`deriveComponents` plus `web/src/components/ComponentConditions.tsx` (Task 7) render a row's highest-severity unresolved condition with a caption reading "(cluster activity while `<name>` installs)" — asserted verbatim in `ComponentConditions.test.tsx`; mutating the string to claim causation fails the test. The namespace-ownership join this document originally worried about (one namespace holding more than one component's objects) turned out not to need solving: attribution rides the engine's own action cursor, not a namespace lookup, so it is correct regardless of how many components share a namespace. What it does not do, deliberately — see "Constraints Phase 3 inherits" above — is claim the row's action *owns* the workload it labels.

**The Pod and Event informers.** `internal/observer` now covers all 5 of the informer kinds `approach.md:95,290-291` name, not 3. `internal/observer/scoped.go` (Task 4) gives Pods and Events a namespace-scoped, lazily-started lifecycle distinct from the three cluster-scoped informers `observer.go` builds once at `Start` — see "New for 2b-iii" below for the shape and why it cannot be simpler. `internal/observer/pods.go` (Task 5) narrates `ImagePullBackOff`/`ErrImagePull` (normalized to one identity — kubelet's two names for one stuck pull), `CrashLoopBackOff`, and `PodScheduled=False/Unschedulable`, tracking a *set* of trouble Reasons per Pod rather than a single value, so a pod that narrates more than one over its lifetime — pending on GPU capacity, then later stalling on an image pull — does not strand the earlier one when the later one overwrites it. `internal/observer/events.go` (Task 6) narrates cluster `Warning`-type Events (a server-side `type=Warning` field selector on their own factory, so it cannot leak onto the Pod `ListWatch`), deduped per (involved resource, Reason), resolved either when the involved Pod recovers (a direct read of the Pod informer's own cache — see the open item in "Deferred findings" below) or is deleted.

**The event-volume calibration question — answered by a property assertion, not a measurement.** "New for 2b-ii" below used to read "whether change-detection alone bounds event volume on a real 8-node driver rollout... is unmeasured." This phase did not get real hardware to measure against, so the real-world number is still open — but it did pin, as a bite-proofed test, the mechanism that number depends on: `internal/observer/pods_test.go`'s `TestPodUnchangedTroubleEmitsExactlyOnce` (and its DaemonSet/Deployment/Node/Event siblings, some pre-existing since 2b-i) asserts that ten identical informer deliveries for one still-broken resource publish exactly one bus event. Reverting the dedupe guard to a bare struct comparison leaves every other test in the package green — this package's own headline constraint, stated in `observer.go`'s package doc ("It aggregates; it never relays"), had no test discriminating a violation of it until this one. The MIG-partitioned-node question from the same "New for 2b-ii" paragraph is unrelated and still open — see below, unchanged.

**`RunScope` atomicity, previously carried in "Deferred findings" as "unfalsifiable by the current test suite."** Closed as a side effect of Task 3; that bullet has been removed from "Deferred findings → Observer" below rather than carried forward, since it is no longer an accurate description of the test suite. `internal/observer/handlers_internal_test.go`'s `TestPublishDoesNotStampAcrossARunTransition` is exactly the test the old bullet said did not exist: a fake scope returns a *different* `RunScope` on its second call within one `publish`, so a two-read regression would produce an event whose namespace filter and Component stamp disagree about which run they came from — this test catches that. `TestPublishReadsAttributionOncePerEvent` independently pins the call count at exactly one. Neither test existed before this phase; both were written for Task 3's own composition, not retrofitted for this deferred item — recorded here because a stale "still true" deferred item costs a future implementer real time re-deriving what these two tests already prove.

---

## New for Phase 3 (Prove)

**`StateActive` is reachable now, so the constraint above that calls it "declared but unreachable" is closed.** `engine.ActiveStep` is the hook: a final step that reports `LeavesWorkloadRunning()` finishes its run at `StateActive` instead of `StateDone`, and `Engine.Stop` — operator-initiated, always — is the only exit. `ReconcileWorkloads` settles the persisted record against the cluster at startup, adopting a workload whose record was lost rather than deleting it. The observer consequence that constraint warns about ("a recovered `StateActive` run would start scoped watches with nothing to tear them down until an operator's Discard") is unchanged and still open: Discard now refuses a run holding a workload, so Stop is what ends the scope.

**Three things the Prove step cannot prove on the only substrate it has, all measured on the KWOK demo cluster rather than reasoned about.** They are repeated in `DEMO.md` and in `NewProve`'s own doc comment, because they bound what the console may claim:

- **The workload body never executes.** KWOK marks a gang pod `Succeeded` in the *same second* it binds it, without starting the container — observed at 10:28:39/40 with the Job's `completionTime` at 10:28:40. `workload.yaml`'s command never runs and its image is never pulled.
- **The gang never holds its GPUs simultaneously**, because each member's resources are released the instant KWOK completes it. That is why both members routinely land on the *same* simulated node here; co-location is a property of the substrate, not a scheduling fault, and `test/e2e/prove.sh` deliberately does not assert "two distinct nodes at 8 of 8" — an assertion the design's own §6 wording implies and that this cluster cannot satisfy.
- **DRA is entirely unexercised.** The workload requests scalar `nvidia.com/gpu`; the fake nodes publish no `ResourceSlices`; the DRA driver the recipe installs is never asked to bind a device.

All three close only on real hardware (Phase 4).

**Four defects reached a real cluster that the whole unit suite called green — the clearest evidence yet for what an e2e is for.** Each was found by `test/e2e/prove.sh`, none was findable against a fake clientset, and all four are fixed: (1) the reference workload tolerated neither taint its own GPU nodes carry (`kwok.x-k8s.io/node=fake` everywhere, `nvidia.com/gpu` on GKE), so kai-scheduler answered *"no nodes with enough resources were found: 4 node(s) had untolerated taint(s)"* and the gang could never place on any cluster this console can demo on; (2) placement was polled every 20ms across a three-minute budget — ~9000 List calls — which client-go's own rate limiter refused long before the deadline; (3) the resulting refusal became the run's *entire* recorded error, hiding the 0/2 placement that was the real diagnosis; (4) `PlacedNodes` excluded `Succeeded` pods, which on a substrate that completes pods instantly meant every simulated run timed out over a gang that had in fact been placed. **The common shape: each is a property of a live API server — taints, rate limiting, deadline propagation, controller timing — and a fake clientset has none of them.**

**Re-applying the recipe onto a cluster where kai-scheduler is already running can leave the running scheduler unable to update PodGroup status.** Observed once on the demo cluster: a second Apply rolled `admission`, `binder`, `pod-grouper`, `podgroup-controller` and `queue-controller` while the 18-minute-old `kai-scheduler-default` pod kept running, and that pod then logged `Failed to update pod group status ...: Unauthorized` at roughly 50/second until it was restarted, with nothing scheduling in the meantime. Recorded as an observation, not a diagnosis — the mechanism was not chased. It does not affect CI (each e2e job installs once into a fresh cluster) and it does affect repeat runs against a live `make demo` cluster, where the remedy is `kubectl -n kai-scheduler rollout restart deploy/kai-scheduler-default`.

---

## New for 2b-iii

**Correlation is temporal, and the copy has to stay honest about that — this is now a hard, tested constraint on any future UI text touching this feature, not a style preference.** `deploy.sh.tmpl`'s own note (~line 488) says cluster convergence continues asynchronously after `--wait` returns — Nodewright alone can take 10-20 minutes on fresh GPU nodes — so an event observed while an action installs is not proof the action caused it. `bus.ClusterData`'s doc comment (`internal/bus/cluster.go`), `Attribution.ActiveAction`'s doc comment (`internal/engine/attribution.go`), and `ComponentConditions.tsx`'s rendered caption all say the same thing in the same words: "cluster activity while `<name>` installs" (present tense) / "installed" (past tense — `Cockpit.tsx`'s `Done` screen passes `tense="past"`, since a condition can genuinely survive to Done and describing an over run as still installing would be its own dishonesty). `ComponentConditions.test.tsx` asserts the rendered string directly; mutating it toward a causal claim fails the test.

**The attribution snapshot updates only after its marker reaches the bus, never before — and the ordering is structural, not a convention to remember.** `internal/engine/engine.go`'s `runStep` builds one `emit` closure per step (`engine.go:595`) that calls `e.bus.Publish(ev)` first and, only for a `KindComponent` event, `applyComponentMarker` → `Engine.setActiveAction` (`internal/engine/attribution.go`) second. Get this backwards and a concurrent `Attribution()` read — the observer, on its own goroutine — could label a cluster event with a deployment action whose header the SPA has not received yet.

**Scoped informers are one factory per namespace, per kind — Pod and Event never share one — lazily started once a recipe resolves, torn down the instant the run that resolved it is over.** `informers.WithNamespace` takes exactly one namespace (verified against the pinned client-go v0.36.3: `factory.namespace = namespace` is a single field, last write wins), so a multi-namespace recipe means one `SharedInformerFactory` per namespace *per kind* — `internal/observer/scoped.go`'s `factoryEntry` holds two (`podFactory`, `eventFactory`), roughly twenty for the current ten-namespace recipe, not one namespace-filtered factory. They cannot share a factory: `WithTweakListOptions` is a factory-level field applied to every informer built from it, and Task 6's `type=Warning` server-side selector on the Event factory would otherwise also apply to the Pod `ListWatch`, where `type` is not a supported field selector — the Pod informer would simply never sync. They cannot be built once at `Observer.Start` the way the three cluster-scoped informers (DaemonSets, Deployments, Nodes) are, either: the observer starts at pod boot, before any run — let alone a resolved recipe — exists. `scopedInformers.reconcile` builds them lazily once `RunScope.Namespaces` is non-empty for a given run and tears every namespace's factories down the instant `RunScope.Terminal` is true (Ruling 3/8: as aggressive as possible, no grace window). **Ruling 38 (whole-branch review finding I3): the accepted cost of that aggressiveness was recorded here as an absence — "the console does not narrate `deploy.sh`'s own post-`done` convergence window" — and that understated it. The actual form is a stale claim frozen visible, not silence.** Teardown (`clearNamespaceState`, `internal/observer/observer.go`) publishes nothing, by design; `withNamespaceLive` then refuses every further write for that run's namespaces, so no resolution can ever be published for whatever was still open at that instant; and the SPA's `tracked` map (`web/src/pipeline.ts`) clears only on the next `run retrying` phase event, never on reaching `done`. **Correction to an earlier draft of this very paragraph, caught by the closing re-review: only the SCOPED conditions freeze, not every condition.** So any Pod-trouble or Event-Warning condition still open at the terminal instant — the two kinds `internal/observer/scoped.go`'s namespace-scoped Pod/Event informers produce — freezes onto its row exactly as it last stood and stays there, unchanged, for the rest of that run's display life, rendering on the Done screen beside "installed successfully" until the operator starts another run. `RolloutProgress` does **not** freeze: it is produced only by `handlers.go`'s `onDaemonSet`/`onDeployment`, registered on the three CLUSTER-scoped informers (DaemonSets, Deployments, Nodes) built once in `Observer.Start` and bound to the process's own `stopCh` — `scopedInformers` never touches them, and `Observer.publish` checks namespace membership but never `sc.Terminal` — so an open `RolloutProgress` (`ready < desired`) keeps updating and resolving normally after `done`, exactly as it would mid-run — it does not freeze (demonstrated directly: the closing re-review's probe updated a DaemonSet 3/8 → 8/8 against a `Terminal: true` scope and it published `Resolved: true` regardless). `test/e2e/apply-real.sh`'s own `SETTLE_SECONDS=180` is evidence for the Pod/Event claim, not the rollout one — it describes the window in which the console is still *narrating and resolving* rollouts, the opposite of frozen. Ruling 3's aggressive-teardown decision itself is not reopened by this correction, only its recorded cost is, and no grace-window fix is proposed or deferred here — Mark's decision and the mechanism both stand; this paragraph now just says plainly what the cost is, and to which conditions it applies. (An adjacent methodological note, since this ledger is closing: Task 6's own fix-wave tests repeated the fixture-blindness pattern named as this phase's defect mode one level deeper than the code they were fixing — the multi-namespace test added for the namespace-discrimination finding varies namespace count but pins RunID and `Terminal: false`; the Retry-vs-new-run tests added for the cross-run leftover finding vary RunID but pin namespace count at one. Neither test varies both dimensions together, which is exactly why `scopedInformers.isRestart` — namespace-keyed AND RunID-scoped, the one predicate sitting at the intersection — kept a namespace-blind mutation (`return len(s.everTornDown) > 0`) surviving through two fix rounds until the closing re-review named it directly; closed in the pre-merge fix that follows. Fixture blindness did not stop being the defect mode this phase kept rediscovering — it moved from "one variable held constant" to "two variables never varied together.") It is level-triggered against `RunScope`, not edge-triggered off a single signal, and driven by two independent wakeups that both re-derive the same reconcile call: the bus's `KindPhase` events as the fast path, and a ticker (`reconcileInterval`, pinned at 2s in production, guarded by `TestReconcileIntervalStaysWithinTheLeakBound`'s 30s leak bound against silently regressing to effectively unbounded) as the correctness floor — `internal/bus` drops live events for a subscriber more than 256 behind with no replay for this internal listener, so a dropped terminal event costs one missed tick, not a permanent leak.

**`engine.Retry` reuses the RunID, and this phase hit three separate defects from something keyed on a RunID boundary that Retry does not actually cross — check this before keying anything new on RunID.** Recorded because it is the single most expensive lesson of the phase: (1) Task 4's first cut inferred run termination from an at-most-once `terminated` sentinel keyed by RunID; Retry reusing the ID left a retried run's informers permanently wedged off. Closed by making `RunScope.Terminal` self-describing and re-derived on every reconcile (Ruling 8), instead of inferred once and latched — a retried run's `Terminal` simply reads `false` again on the very next call, no separate "un-terminate" signal needed. (2) Task 6's Event dedupe was briefed to bound itself by `engine.Attribution.Generation`; verified wrong before it shipped — `Generation` advances on every `ActiveAction` transition (~28 times over one ordinary 14-action run) and never resets for the process's life, so keying eviction to it would neither distinguish a new run from the old one nor avoid flushing the dedupe map roughly twice per action. Closed by keying eviction to the informer teardown/restart cycle instead (`clearNamespaceEvents`, composed with `clearNamespacePods` into `clearNamespaceState`), which fires on the terminal-scope branch precisely because Retry crosses *that* boundary even though it never changes RunID. (3) The console's own re-narration suppression (`onPodAdd`/`onEventAdd`'s `isInInitialList` handling) originally suppressed every informer's initial list unconditionally — correct for a console *restart* (a genuine first sighting) but wrong after a Retry's teardown-then-restart within the *same* RunID, where a pod still wedged on the identical reason across a retry would have published nothing for the whole retried attempt. Closed by `scopedInformers.isRestart` (Ruling 32, backed by `everTornDown`), which distinguishes "this process has never torn this namespace down" from "this process tore it down and is now watching it again," narrating in the second case exactly as it would a genuinely new occurrence. Ruling 32 as shipped kept `everTornDown` for the process's whole life, reasoning a brand-new run reusing a namespace name "qualifies identically" to a Retry — the whole-branch review (finding I2) showed that reasoning does not survive Kubernetes' own 1h Event TTL: a Warning from run 1, still present when run 2 starts because it has not yet expired, has no resolution path under run 2 (the pod it was about is gone, so neither the recovery-edge nor the cache-pull path below can ever retract it), so it re-narrated onto run 2's rows as a fresh, permanently-stuck condition. **Ruling 37** scoped `everTornDown` to the RunID it was earned under instead — cleared the moment `reconcile` sees a genuinely different `RunID`, preserved across a same-RunID Retry — which keeps this paragraph's fix exactly as described for a Retry while dropping the cross-run carry-over the review found.

---

## New for 2b-ii

**The applier's marker parser (`internal/applier/parse.go`) is a maintenance liability, pinned only by `TestDeployTemplateUnchanged`.** It transcribes `deploy.sh.tmpl`'s `printf` output formats into seven hand-written regexes; nothing in the Go type system connects the two. `TestDeployTemplateUnchanged` (`internal/applier/template_test.go`) pins the upstream template's sha256 (`df919af7e46d565d38fbf12927881ebeec1172227efac8962e4c00f035a8b519`) via `go list -m -f '{{.Dir}}' github.com/NVIDIA/aicr` against the local module cache, so any upstream edit to that template fails this test loudly instead of silently drifting. The freed-up upstream-PR budget from an earlier resolved risk should go toward adding an opt-in machine-readable event stream (`AICR_DEPLOY_EVENTS=jsonl` or similar) to that template — that is what retires the parser and this whole maintenance surface, rather than deepening it. See the AICR bump checklist below for what to actually do when this test fires.

**Image size: watched in 2b-i, no step change.** `Dockerfile` was not touched anywhere in Phase 2a (verified: `git diff 460f7b4..cfe5daf -- Dockerfile` is empty) — 2a added no new binaries or base-image layers to the shipped image; Task 1's `cmd/probe` that pulled in AICR's Go module graph was a throwaway, deleted before its own commit. This section used to say "watch it in 2b," flagging that linking `client-go`'s informer and typed-client machinery for the observer was a plausible step change. It was watched: Task 5 measured a `git stash`-isolated before/after image pair directly. Uncompressed (`docker inspect --format '{{.Size}}'`): 203,642,237 → 203,707,773 bytes (+64 KiB, +0.03%). Compressed (`docker save | gzip | wc -c`): 55.6 MB → 55.6 MB (+12.5 KB). No step change — `k8s.io/client-go`, `k8s.io/api`, and `k8s.io/apimachinery` were already linked into the binary via `github.com/NVIDIA/aicr`'s own Job/ServiceAccount/RoleBinding management (`pkg/k8s/agent`) before this phase, so `internal/observer`'s additional imports (`informers`, `tools/cache`, three typed listers) were incremental on already-linked code, not a fresh pull of the whole dependency tree. The ~55 MB compressed figure holds. The ConfigMap store and any Prove/Reset tooling in 2b-ii are still candidates to grow it — watch it again then.

**The observer's remaining open calibration question, a Phase 4 measurement.** `internal/observer/handlers.go`'s `gpuResource` constant tracks only `nvidia.com/gpu`; whether MIG-partitioned nodes' resource names (e.g. `nvidia.com/mig-1g.10gb`) need the same allocatable-diff treatment is open, because no MIG-partitioned node exists in KWOK to exercise it. Needs real GPU hardware to answer. (The sibling question this paragraph used to carry — whether change-detection alone bounds event volume on a real driver rollout — is closed on the *mechanism*, if not yet the real-world number; see "Resolved in 2b-iii" above.)

**`workDir.sizeLimit` is `1Gi`** (`charts/aicrme/values.yaml`'s `workDir.sizeLimit` key), chosen deliberately generous against Task 1's measured 352K real bundle — the reasoning documented at the site is that helm's own chart cache for 14 charts, not the bundle itself, is expected to be the larger consumer of that emptyDir in practice. Override via `--set workDir.sizeLimit=<value>`; verified to flow through to the rendered emptyDir.

**The `InvalidDefaultArgInFrom` Docker build warning is the mechanism working, not a defect.** `docker build` warns because `Dockerfile:6`'s `ARG GO_VERSION` has no default. `Dockerfile:2-5`'s own comment explains why: `.go-version` is the single source of truth for the toolchain (per `CLAUDE.md`), so an unset build-arg must fail the build (`FROM golang:-alpine` is an invalid reference) rather than silently drifting from it — `make image` is what supplies the value. Someone will eventually see this warning in a build log and "fix" it by adding a default, which would quietly reintroduce the drift the missing default exists to prevent. Leave it.

**`strategy: Recreate` costs an upgrade its uptime.** The chart sets it precisely so the ConfigMap store can never have two writers live at once (see "Resolved in 2b-ii" above), but the consequence is real, not free: every `helm upgrade` tears the single running pod down before starting its replacement, so the console is unreachable for the gap in between. Acceptable for a single-operator demo console. Anything that later wants a zero-downtime upgrade needs leader election first, not just more replicas — `Recreate` plus optimistic concurrency is only the proportionate answer as long as there is exactly one writer by construction.

**The envelope is versioned, and a format change has to decide what a previous-image record means.** `envelopeVersion` (`internal/engine/envelope.go`) exists so a format change is safe to roll out against a ConfigMap a previous image wrote: `decodeRun` refuses an envelope whose version does not match exactly what this build writes, rather than guessing at a partial decode. That refusal is not silent today — `Recover` treats an undecodable or wrong-version record the same as any other unreadable one (`markStoreUnreadable`: swap to a fresh memory store, leave the ConfigMap untouched, log at Error). A version bump still needs its own explicit decision, not this default by inheritance: whether a record written by the previous image should be migrated, discarded, or left for an operator to `kubectl delete` by hand.

**The 800 KiB guard is a guess, not a measurement.** `maxPayload` in `envelope.go` is sized against the only figure this project has: 66-73 KB compressed, from the KWOK snapshot fixtures. A real cluster's snapshot — more nodes, more real device and driver detail — is untested against it. The guard's job is to turn a wrong guess into a legible, fail-closed error (`"run state too large to checkpoint"`) instead of a corrupted run or an opaque API-server rejection, and it does that regardless of whether 800 KiB turns out to be generous or tight. Phase 4 on EKS is the first honest measurement.

**A live client that cannot resolve its own Deployment fails almost silently.** `newRunStore` (`cmd/aicrme/main.go`) logs at Error, not Warn, when a real cluster client cannot look up the Deployment named by `AICRME_DEPLOYMENT_NAME` — RBAC, a control-plane blip, an unusual install order — and falls back to an in-memory store (a nil client, the expected shape for `make build && ./bin/aicrme` on a laptop, logs at Warn instead — a different, non-anomalous case). `/healthz` is pure liveness and reports identically healthy either way, so that Error line is the only signal an operator has that the durability this whole phase exists to provide has quietly gone missing.

---

## AICR bump checklist

`aicr` is pinned to `v0.19.0` in `go.mod` and `.settings.yaml`. An AICR bump is expected and acceptable — the phase deliberately keeps the production code free of catalog-count literals (verified: zero component-count literals across `internal/**` and `cmd/**` at the end of 2a) — **provided the response to each signal below is the right one.** This table exists because the detectors already exist; what was missing before this phase is the documented response.

| Signal that fires on a bump | What it means | Correct action |
|---|---|---|
| `make check-aicr-pin` fails | `go.mod`'s `github.com/NVIDIA/aicr` version, or the snapshot agent image tag (`defaultSnapshotAgentImage` in `cmd/aicrme/main.go`), disagrees with `.settings.yaml`'s `dependencies.aicr` | Bump both together, deliberately. This is the pin-consistency check, not a functional test — it is telling you the two places that must agree, have not been updated together. |
| The `succeeds` (intent, platform) matrix in `internal/steps/recommend_test.go` fails | The catalog's coverage — which intent/platform pairs actually resolve on KWOK — changed | Re-verify which pairs resolve against the new catalog and update the matrix to match reality. This test exists to catch catalog coverage *drifting silently*, not to gate the bump — a shrinking or growing resolvable set is real information to record, not a regression to suppress. |
| `TestDeployTemplateUnchanged` fails (`internal/applier/template_test.go`) | The upstream `deploy.sh.tmpl` this phase's marker parser transcribes has changed | **Re-read the upstream template's output helpers against `internal/applier/parse.go`'s regexes and think.** This is the one pin whose correct response is *not* "update the number." Updating the sha256 reflexively to turn CI green would silently empty the Apply timeline in production — every marker line the new template emits differently would simply stop matching, and every existing test would still pass, because the fixtures pinning today's grammar would need to be re-captured too. If the template only added something the parser can safely ignore, update the sha256, confirm the regexes still match by hand, and move on. If it changed a format the parser depends on, update the regexes *and* re-capture `internal/applier/testdata/deploy-transcript-kwok.txt` against the new template before updating the constant. |
| `assertMatchesApproved` (`internal/steps/bundle.go`) fails at runtime | The catalog's actual component composition for an already-approved recipe changed between Recommend-time and Bundle-time re-resolve | This is the fail-closed guard working as designed — it exists precisely to catch a bump changing what a stored `recipe.json` resolves to. It should not be silenced; if it fires in production against a real bump, the run correctly refuses to install a component set the user never saw and reviewed. The guard compares all six `ComponentSummary` fields (`Name`, `Kind`, `Version`, `Namespace`, `Chart`, `Source`) per component, plus the recipe's own `Name`/`Version` — not just component name and version, which is what it checked before a final-review fix widened it. The error names every field that differed, not just that drift occurred, so a bump investigation starts from the log line rather than the code. |
| The e2e's pinned ceiling (`test/e2e/apply-dryrun.sh`'s `EXPECTED_FAILING_COMPONENT="network-operator"`, `EXPECTED_FAILING_INDEX="3"`) fails | Either the recipe's component ordering shifted (a bump moved `network-operator`'s position without changing the underlying CRD-ordering issue) or the underlying nfd/network-operator CRD-ordering limitation was actually resolved upstream | Read the script's own failure message before touching the assertion — it names exactly which of these happened. A position shift with the same underlying error is a benign catalog reshuffle; update the pinned index. A run that now reaches `done` under dry-run is a genuine upstream fix and needs a human to re-verify and rewrite this section of the handoff, not just widen the assertion. |

**Explicit non-goal, still open.** `approach.md` Risk 5 asks for "a deliberate bump cadence, and probably a CI job that opens a bump PR when aicr releases." Phase 2a builds none of that, and 2b-i didn't touch it either — the checklist above is manual, human-triggered process, not automation. Keeping this as an explicit non-goal (rather than a silently-dropped idea) is deliberate, so it stays visible for 2b-ii or later to pick up rather than being rediscovered from scratch.

---

## Deferred findings

Every per-task review's minors, triaged as acceptable to defer past 2a. Grouped by where they bite.

### Still open after 2b-ii's ConfigMap store / restart-recovery work
- `StateActive` unreachability (above, "Constraints Phase 3 inherits") — still Phase 3's job, and 2b-iii found a consequence waiting on the other side of it — see "Deferred findings → Observer" below. Unlike the other two items originally grouped here, this one's reachability depends on Reset existing, not on the store being real, so neither 2b-ii nor 2b-iii landing activated it.
- The other two items this heading used to group — the `bus.nextID` epoch reset and Finding 1's required-contract list — are closed; see "Resolved in 2b-ii" above for both, itemized.

### Applier / process execution
- `internal/applier`'s `Applier.env` method does not use its receiver; could be a plain function.
- `parse.go`'s `atoi` comment calls a parse failure "unreachable"; strictly, RE2's `\d+` has no length cap, so a pathological digit run could overflow `int`. Overstatement, not a defect.
- `TestParseTranscriptFixtures` asserts only presence/absence of each marker Status across a transcript, never counts — it catches a regex regressing to match nothing, but not a partial regression (13 of 14 headers still leaves `seen[StatusStarted] == true`). The captured kwok fixture's marker counts (14 headers, 14 installed, 0 failed, 0 retrying, 2 phase-ok, 1 async) are pinned as an exact-count assertion specifically because that fixture is a frozen file — 14 is a property of the artifact, not of AICR's catalog, so pinning it cannot break on a bump.
- Two `internal/applier` branches were left deliberately covered only by artificial (not naturally-reachable) tests, both documented at their code sites and both intentional, not oversights: the SIGKILL escalation branch in `exec.go` (needs `killGrace` shrunk via the test-only package var to exercise sub-second) and the engine's epoch guard (above).
- `internal/engine`'s `Start` Save-failure rollback path had zero test coverage as of Task 7's initial pass (caught by a coverage-percentage *drop* rather than by reading, since the new branch was entirely uncovered); it was fixed alongside `Retry`'s identical rollback in the same fix round (Ruling 13), so this is resolved, not deferred — listed here only so its discovery method (coverage signature, not inspection) is on record as a technique worth repeating.

### API / bundle download
- If the bundle root directory itself became a symlink between `validateBundleTree`'s pre-validation pass and `writeBundleTar`'s streaming pass, the inline second-pass check in `writeBundleTar` would miss it (it short-circuits at `rel == "."` before the type check); `validateBundleTree` does check the root. An unrealistic TOCTOU window given the bundle directory's single-writer ownership (the in-process bundler, one run at a time), and not a regression — pre-fix code never checked it either.
- The defense-in-depth comments around the symlink-rejection logic do not spell out *why* the TOCTOU race is considered unrealistic (single-writer bundle directory); worth one added line next time that file is touched.

### API / SSE shutdown cost
- **An open SSE stream makes every pod termination cost the full `httpShutdownTimeout`.** `internal/api/events.go`'s `handleEvents` returns only on `r.Context().Done()`, and `http.Server.Shutdown` does not cancel in-flight request contexts — it only stops accepting new connections and waits for active ones to go idle. A live SSE stream is never idle by that definition, so `httpSrv.Shutdown` in `cmd/aicrme/main.go` always burns its full 10s budget rather than returning as soon as the last real request finishes. Not a correctness bug — the wait still fits inside `runShutdownTimeout`/`terminationGracePeriodSeconds` because the two shutdown goroutines run concurrently — it just makes every `helm upgrade` on this release about 10s slower than it needs to be, and the error `httpSrv.Shutdown` returns on timeout is silently discarded (`_ = httpSrv.Shutdown(httpCtx)`), so today there is no log line to notice it by. Fix shape: `httpSrv.RegisterOnShutdown` closing a channel that `handleEvents` also selects on, so an open stream unblocks itself the moment shutdown begins instead of waiting for the client to disconnect; and stop discarding the error.

### Observer
- **Informers retain full objects cluster-wide under a 512Mi memory limit.** `internal/observer/observer.go`'s `informers.NewSharedInformerFactory` call is unscoped to a namespace and installs no transform, so the local cache holds every DaemonSet, Deployment, and Node in the cluster in full — including `metadata.managedFields`, the complete `spec.template`, and (for Nodes) `status.images` — against the chart's `resources.limits.memory: 512Mi` (`charts/aicrme/values.yaml`). Cluster-wide *is* the right scope: the namespaces a run installs into are not known until Recommend resolves a recipe, well after `Start` has to begin watching. A `cache.SharedIndexInformer.SetTransform` stripping `managedFields` and the pod template before objects enter the store would cut the per-object footprint several-fold for a few lines, without narrowing what is watched. **2b-iii's Pod and Event informers (`internal/observer/scoped.go`) do not carry this same cost** — they are namespace-scoped by construction (`informers.WithNamespace`, one namespace at a time, built only once a recipe resolves), so their cardinality is bounded by the run's own namespace set rather than the whole cluster. They install no transform either, so the same stripping idea applies if their footprint ever needs trimming — untested, since no 2b-iii task measured it.
- ~~**Resolution covers Pod-involved Warnings only.**~~ **Mostly closed 2026-08-21**, after this exact item was the first thing a human hit on the first `make demo` (`docs/ux-feedback.md` item 2: a red `FailedCreate` frozen on a green success screen, minutes after it had converged). The recovery signal the original note said "none of them have today" was already in hand: a pod's controller `ownerReference` carries the same Kind/Name/UID the Warning's `InvolvedObject` does, so `onPodChange`'s full-recovery branch now also resolves `controllerEventKey(pod)` — the same shared-identity argument Ruling 23 made across the Pod/Event boundary, one level up the ownership chain, with no new informer. **Still open:** a Warning involving a **Deployment** or a **Node**. A Deployment's pods are owned by its ReplicaSet rather than by it, and a Node owns nothing, so neither is reachable this way. Both are rare in practice (a Deployment's own events are almost entirely Normal `ScalingReplicaSet`), and a second mechanism was judged not worth it — the lesson worth carrying is not the residue but that "no recovery signal exists" was wrong for the two kinds that actually mattered, and stayed wrong for two phases because nobody looked at ownerReferences.
- **The Event handler reads the Pod informer's cache directly — new coupling between two informers that were independent through six tasks.** `internal/observer/scoped.go`'s `currentPod` is a PULL against the Pod informer's own indexer (`entry.pod.GetIndexer().GetByKey`), called from `events.go`'s `onEventChange` to answer "is this pod healthy right now" for a Warning with no future Pod transition ever coming to resolve it otherwise (Ruling 26). It is non-reentrant by construction — `currentPod` releases `s.mu` before touching the indexer, and its one call site in `events.go` sits outside the `withNamespaceLive` closure that also needs `s.mu`, with the ordering stated at the call site — and was audited for deadlock risk when it was introduced. Recorded as open, not a bug: it is still a new cross-informer read the Pod and Event informers went without through six tasks of this phase, and it means the Event path's correctness now depends on the Pod informer's cache being populated, not just its own.
- **A recovered `StateActive` run would start scoped watches with nothing to ever tear them down until Discard.** `internal/engine/attribution.go`'s `Terminal` is `StateDone || StateFailed`, deliberately not `!isLive` (Ruling 10 rejects that — it would misclassify `StateIdle`/`StateActive` as terminal, tearing down a live Prove workload's watches the day that state is reachable). `StateActive` has no producer today (see "Constraints Phase 3 inherits" above), so this is unreachable — but the day Phase 3 wires the Prove workload, a restart recovering a run sitting in `StateActive` would leave `scopedInformers.reconcile` treating its namespaces as live indefinitely, since nothing in the engine currently drives that state toward a terminal one on its own. Whoever wires Prove owns closing this; recorded now so it is not rediscovered as a surprise leak later.

### main() shutdown composition
- **`main()`'s shutdown composition has no test.** Drain-before-wait (`srv.Drain()` called before the two shutdown goroutines start), the two-goroutine `sync.WaitGroup` running HTTP shutdown and `eng.CancelAndWait` concurrently, the deferred `close(obsStop)` / `stop()` / `client.Close()` ordering, and the observer's asynchronous `Start` are all verified only by reading `cmd/aicrme/main.go`. `srv.Drain`, `eng.CancelAndWait`, and `newRunScopeFn` are each unit-tested in isolation in their own packages; nothing exercises their composition in `main`, so a future reordering of that block (e.g. draining after the wait group instead of before, which would let an unguarded `POST /api/runs` race shutdown) would not fail any existing test.

### Cockpit / frontend
- `currentRunIdOf`/`relevantTo` run-id filtering logic is duplicated between `Wizard.tsx:103-108,119-120` and `pipeline.ts:293-303` (line numbers as of 2b-iii — `pipeline.ts` grew by ~280 lines this phase, and Task 7 added a second caller of the same pattern, `deriveComponents`, rather than a third copy) — a handful of lines that can drift out of sync. Justified for now by not relocating `deriveRunState`/`RunState` out of `Wizard.tsx` (a deliberate scope decision), but a future task consolidating shared run-state helpers should fold these together. Correction: the previous note here cited an `OVERRIDE 2` code comment as the reason for the split; no such comment exists anywhere in the repo (verified by grep) and git history shows none ever did — a stale citation, not a stale decision. The decision itself still holds.
- `ComponentRow` does not surface a per-row index; only the aggregate progress line shows the running total.
- The session probe (`useEvents.ts`, wired to `GET /api/session`) has no in-flight dedup — the intent is "probe once per connection drop, not once per `EventSource` retry tick," and today that holds only incidentally (bounded by `EventSource`'s own ~3s retry cadence against an O(1), no-I/O endpoint), not because anything enforces it explicitly.
- `TestAnalyzeBareSnapshotIsSafe` does not assert `Analyzed == false` on the inner-`nil` degraded branch; the property is structurally guaranteed by the code shape but left uncovered by an explicit assertion.
- No App/Console render-level test asserts the `EventSource` instance count stays at 1 across a `Console` re-render. Correct today, but a latent trap: any future App-level state added while authenticated could silently break the stable-reference invariant the whole live-timeline UX depends on, and the failure mode would present as an intermittent, hard-to-explain "flaky backend" rather than a clear test failure.

### Chart / contract test cosmetics
- `test/chart/contract.sh`'s Invariant 6 banner wraps to two lines, unlike every other single-line banner in the file.
- `contract.sh`'s `has("emptyDir")` assertion is redundant with the separate `sizeLimit` assertion — no independent failure mode between the two.
- `readOnlyRootFilesystem` landed in its own new Invariant 6 rather than folded into the existing "Invariant 5: pod hardening" block; defensible either way, not revisited.

### E2E scripts
- Two idioms for the same SSE-truncation problem now coexist across the file family: `discover-recommend.sh` bundles the events fetch and its parse in one `set +e` block, while `apply-dryrun.sh` pre-filters with `fromjson? // empty` and then runs under plain `set -e`. Both are correct and the divergence was an explicit, reasoned choice (Ruling 17), but a future task may want to converge on one idiom across both scripts.
- `apply-dryrun.sh`'s `ERROR_TAIL` handling (`.data.tail[]`) would abort under `set -e` if `.tail` were ever a non-array truthy value. A pre-existing shape assumption inherited from the SSE event schema, not introduced by this phase.
- The in-image kubectl version is captured and printed by `apply-dryrun.sh` but deliberately not asserted (only the helm major is), which reads as if it were an assertion; worth a comment labeling it informational.

### Cosmetic / low risk, carried unchanged from Phase 0-1 (still true, still low priority)
- `.golangci.yaml` carries orphaned `gocognit`/`gocyclo` settings for linters not enabled.
- `/` answers all methods, so `POST /` and `POST /healthz` return `index.html` 200.
- `isAssetPath` heuristic: false-positive on a client route containing a dot, false-negative on an extensionless asset.
- CSP lacks `base-uri`, `form-action`, `object-src`. `index.html` is not content-hashed and gets no `Cache-Control`.
- `useEvents.ts` comment claims an O(1) fast path but the append is an O(n) copy; it closes the closure `source` rather than `msg.target`.
- `gap` rules: `gpu-scheduler` covers KAI images only (`kueue`/`grove` are also gang-scheduling adjacent); a comment cites the chart name `aws-efa-k8s-device-plugin` where the component is `aws-efa`; `usableGPUs` is all-or-nothing.
- `ServiceFromSnapshot` is production-dead (test-only callers). `pairResolves` sits at 81.8% — the gap is two registry parse-failure returns unreachable from real catalog candidates, left honestly uncovered.
- No dedicated failure-state screen for errors outside the Apply pipeline; they render as a text line.
- Chart: helm/kubectl downloads are TLS-only with no checksum; `alpine` pinned by tag not digest; the ClusterRoleBinding uses the cluster-scoped name `aicrme`, so a second install in another namespace fails closed; an operator-supplied `--set auth.password=` is persisted in helm release values and shell history.
- Only 4 of the spec's 5 gap rules ship: `gpu-driver`, `device-plugin`, `gpu-metrics`, `gpu-scheduler`. EFA is deferred because `TypeNetworkTopology` is gated behind the agent's `--discover-network` flag (plumbed through `DiscoverConfig` but off by default) and no fixture exercises it. Two residual false-positive paths exist on `gpuOperatorAbsent`: a policy-specific collector failure defeats the degraded guard, and a standalone helm install of `nvidia-device-plugin`/`dcgm-exporter` with no ClusterPolicy makes both gaps fire falsely. Both are outside the "vanilla cluster" premise.
- The demo's headline number — "N of M GPUs usable" — is unit-tested but no fixture exercises it, because even the simulated-H100 KWOK snapshot reports `gpu-present: false` (the agent finds no real device). It first runs for real on EKS in Phase 4.

### Deliberately not carried forward from the raw ledger
The 5.88 MB captured transcript fixture (`internal/applier/testdata/deploy-transcript-kwok.txt`) and the `kind v0.32.0` vs. `.settings.yaml`'s pinned `v0.31.0` mismatch were both raised and reviewed mid-phase, then judged genuinely low-priority and not worth a line here beyond this note: the transcript is large (596 KB compressed) but was independently judged the deliberately-correct artifact by review (it guards the marker parser against false-positives on real helm noise that a hand-authored fixture cannot reproduce), and the kind version mismatch has no enforced consequence anywhere in `Makefile` or `.github/workflows`. Both are cheap to revisit if they ever start actually costing something; neither warrants tracking as an open deferred item.

**The transcript fixture's real cost, with the number that makes the dismissal above defensible:** 5.88 MB on disk, ~600 KB packed, against a `.git` of 3.8 MB at the time it landed — a one-time ~15-20% history growth. The durable decision rule: **one such fixture earns its cost; a second is the point to reconsider.** Its value is "real helm noise the marker parser must not false-positive on," and that value does not double by capturing a second real transcript from a different scenario — a second fixture would cost another ~600 KB of packed history for marginal additional guard strength. This matters concretely because the AICR bump checklist above tells a future maintainer to *re-capture* this exact file when `TestDeployTemplateUnchanged` fires on a format-relevant upstream change: re-capture means overwrite `internal/applier/testdata/deploy-transcript-kwok.txt` in place (same path, one fixture), not add a second, differently-named transcript alongside it. Each re-capture still re-spends roughly ~600 KB of packed history (a new capture rarely deltas well against the old one), so replace, don't accumulate.

---

## The phase's dominant defect mode: tests that could not detect the thing they were written for

Worth its own heading because it recurred across nearly every task in this phase, in the same shape each time. Carry the practice into 2b-ii, not just the list above — 2b-i already did: Task 5's fix round verified both of that phase's defects (the async observer-start fix, the stale-namespace-cache fix) by mutation, confirming the specific regression test failed with the expected message before calling either done.

The pattern: a test existed, exercised real code, and passed — but its passing proved less than it appeared to, because the specific failure mode it was meant to catch could not actually manifest through it. Instances, in order encountered:

- A golden transcript test (`TestParseTranscriptFixtures`) that could catch a regex regressing to match *nothing at all*, but not a *partial* regression — 13-of-14 headers matching still satisfies "started == true."
- An environment-merge test (`TestBashExecRunHonorsDirAndMergesEnv`) that asserted an injected variable was visible in the child process — a property true under both "append to the inherited environment" and "replace it entirely." It could not distinguish the two, even though the difference is exactly the one that matters: `deploy.sh` and every `install.sh` it calls depend on the *inherited* environment for `PATH` (to find `helm`/`kubectl`) plus the work-dir variables Task 3 added. A silent regression to replace-semantics would have erased all of them and surfaced in production as a baffling helm error, never as a test failure. The fix required planting a second, *inherited* marker via `t.Setenv` and asserting it survives alongside the injected one.
- A cancellation test (`TestBashExecRunCancelKillsGrandchild`, pre-Task-5b) built against a leaf process (`sh -c 'sleep 30'`) with no children — structurally unable to observe an orphaned grandchild, which is exactly the class of bug Finding 4 above turned out to be.
- An epoch/supersession guard (`internal/engine`'s epoch check) that no black-box test can reach at all, because the precondition for triggering `Retry` (`State == Failed`) is set exclusively by the very goroutine whose supersession the guard exists to prevent — by the time any external caller could race it, there is nothing left to race. Proven correct only by a white-box test that manufactures the condition directly.
- A recipe-classification test (Finding 2's `deriveComponents`) that would have passed with **no classification logic at all**, if its fixture had used only marker names present in both the recipe and the transcript — the reviewer required it to include a name (`kubeflow-trainer-post`) genuinely absent from `recipe.json`, taken from the real captured transcript rather than invented, specifically so the test could fail if the classification logic were deleted.

The controller's own verification methods failed the same way, three separate times, worth naming so the next phase doesn't repeat them: a targeted mutation for a bite-proof that didn't compile (an unused import left behind after the mutation, so the "failing" test never actually ran); a `go test` run without `-v` that showed only a failure, proving nothing about which *other* tests still passed (an asymmetric bite-proof requires knowing that, not just that the target test failed); and a `perl` one-liner mutation that silently matched nothing and left the file byte-identical, which would have "proven" a guard untestable purely because the tool used to attack it never actually touched it.

**The practice that caught every one of these before it shipped: bite-proofing.** Reintroduce the bug (by direct mutation, not by argument), confirm the specific test fails with the expected message, restore the code, confirm the suite is green again — and, critically, confirm that the *right* test failed while every other test in the suite stayed green. That last clause is what an asymmetric bite-proof buys over a simple pass/fail check: proof that the new test covers ground the *other* tests structurally cannot, not just that it can fail somehow. Every fix round in this phase that mattered was verified this way, and in several cases (the SIGKILL escalation branch, the epoch guard, the env-merge test) the first bite-proof attempt was itself flawed and had to be redone before it was trustworthy. Treat a bite-proof's own result with the same skepticism as the code it is testing — verify it was set up correctly before trusting what it reports.

### 2b-iii: the same pattern, and the number worth calibrating against

By Task 2 of 2b-iii, the running count of "a test existed, exercised real code, passed, and proved less than it appeared to" instances across every phase to date reached **thirteen** — recorded at the moment it happened (Task 2's own report caught its own ordering test passing 10/10 *with* the bug present, its author's fifth self-caught instance of the class). The pattern did not stop there, and two further shapes of it are worth a future implementer's attention specifically:

- **A whole task's test suite, not just one test, missed the point three separate ways at once.** Task 4's review independently mutated its core deliverable three times — dropping `informers.WithNamespace` (every "scoped" informer silently watches the whole cluster instead), dropping `close(e.stop)` (informers forgotten, never stopped), dropping `factory.Start` (informers never start at all) — and all three left every one of the task's 35 tests green. The suite asserted map bookkeeping (`len(entries)`, pointer distinctness), which stayed true under all three mutations; nothing asserted that a namespace was actually being watched, actually stopped, or actually started. Task 5's own equivalent — three sequential deep review rounds, each closing a real defect the previous round's fix had not caught — is the same shape stretched across an entire task rather than one commit.
- **"Stranding"** — a condition tracked under one identity, resolved only through a *different* identity or scope, so the original identity's entry survives its own resource recovering, being deleted, or moving off-screen — was closed **six separate times at increasing altitude** across Tasks 5-7 by the ledger's own running count, re-emerging each time because the fixture that caught the previous occurrence held constant the one variable that would have revealed the next. The named closures worth a future reader recognizing on sight, roughly in the order they landed (not a claim that this list's numbering matches the ledger's internal count exactly): a single raw Reason per Pod stranding an earlier Reason when a later one overwrote it (Task 5, Ruling 14(b)); the `ErrImagePull`/`ImagePullBackOff` alias pair stranding whichever raw name kubelet had abandoned (Task 5, Ruling 17, replacing 14(b) rather than patching it); tracking only the *current* condition instead of the full set a Pod can accumulate over its lifetime (Task 5, Ruling 20 — "track a set, not a value" is the fix that finally held); the Pod/Event informer boundary, where a Warning had no recovery signal of its own at all (Task 6, Ruling 23); the SPA folding condition identity under the deployment-action Component instead of keeping it flat on (UID, Reason) (Task 7, Ruling 27); and placement itself moving to a later row on an unresolved re-observation even after identity was fixed (Task 7, Ruling 30). The sub-pattern inside that pattern is worth naming on its own: within Task 7's review sequence alone, a fixture that quietly held the one variable constant that would have exposed the bug it was meant to catch recurred at least twice in immediate succession — first every cluster-event fixture held `Component` constant across a sequence (hiding the Critical Ruling 27 fixes), then, even after that was pointed out and the reviewer explicitly asked what today's fixtures hold constant, the *next* round's new cross-component fixtures set `resolved: true` on the second event, making the unresolved-recurrence case Ruling 30 needed unreachable — explicitly logged in the ledger as "third time in this phase a fixture held constant the one variable that mattered." Treat "the stranding class is closed" as a hypothesis to attack, not a settled fact, the next time any per-key tracking map in this codebase is touched — and treat a passing fixture as a claim to interrogate ("what does this fixture hold constant that the real system does not?"), not evidence on its own.

---

## Test gaps are recorded, not blocking (standing rule, stated 2026-08-19)

**If something cannot be fully verified on KWOK, capture it as a named test gap and keep
going.** Do not stall a feature, and do not contort the design to make a simulated cluster
able to prove something it structurally cannot. Every release gets exercised on a real
cluster before it ships, so a recorded gap is a thing verified later — not a thing shipped
unverified.

What this rule requires in exchange: the gap must be **written down where the next person
will find it** — in this document and next to the code — naming what is unverified and what
would verify it. An unrecorded gap is the thing this rule is not licensing.

**One distinction it does not erase.** A test gap is *"we do not know."* A false pass is
*"we are told something untrue."* The second is worse and is not covered by this rule — which
is why Validate stays deferred above rather than shipped with a gap note: `ValidateState`
does not fail to verify on KWOK, it reports `passed` for components that are not installed.
Shipping that would put a green check next to a lie. If a future change makes those results
merely *unavailable* on simulated clusters rather than *wrong*, this rule applies and Validate
should ship with a recorded gap.

---

## Explicit non-goals — do not "fix" these

Air-gapped operation, private registries, and registry mirroring are spec non-goals; direct internet access to ghcr.io, nvcr.io, and the upstream Helm repos is an assumed precondition. `AICRME_SNAPSHOT_IMAGE` is deliberately not exposed in `values.yaml` for this reason. Day-2 operations, multi-user auth/OIDC/scoped RBAC, multi-cluster, and in-UI editing of component sets or versions are all out of scope. The `cluster-admin` grant is deliberate and disclosed; do not attempt to narrow it.

---

## How to start Phase 3

Write a plan first — `superpowers:writing-plans`, informed by this document (particularly "Constraints Phase 3 inherits" above) and the "Open questions" sections of the Phase 2b-ii and Phase 2b-iii design specs — the latter's §1 ("Exact ownership, deliberately deferred") and its Out-of-scope note on the recovered-run screen are both direct inputs to what Phase 3 might pick up. Then execute it; `superpowers:subagent-driven-development` was used for Phase 0-1, Phase 2a, Phase 2b-ii, and Phase 2b-iii and worked well every time. Phase 2a stayed serial from Task 7 onward rather than parallelizing the one genuinely-parallel pair (Tasks 9 and 10) because per-task review packages are `BASE..HEAD` diffs — two implementers on one branch interleave commits, and each review would then see the other task's changes and be unable to attribute findings to the right task. Worktree isolation fixes that but costs a second `npm ci` and a merge with likely frontend conflicts; 2b-iii's Tasks 7 and 8 (disjoint `web/` vs `test/e2e/` trees, explicit stay-in-your-lane constraints in each dispatch, no worktree) show the alternative works too when the file sets are genuinely disjoint — weigh the trade-off explicitly rather than defaulting either way.

Five process notes carried forward now, one from each phase so far. From Phase 0-1: verify a plan's own code before trusting it — four real defects shipped in that phase's plan text and were caught only by implementers. From Phase 2a's own plan: the same thing happened again, smaller — an early draft of Task 7 reached for an `Engine.Abandon` method the phase had no reason to add, caught before dispatch. From across all of Phase 2a: demand bite-proofs, and read the bite-proofing section above before assuming a green test suite means what it appears to mean — the single most common real defect in this phase was not incorrect code, it was a test that could not have caught the incorrect code if it had shipped. From 2b-i, the same family one level up, twice over: a comment can retire a question by documenting the wrong hazard instead of the real one — `newRunScopeFn`'s cache comment reasoned correctly about `recipe.json` never mutating after it is written and missed the pre-write window that actually mattered, which silently dropped every Apply event for a run unlucky enough to publish before Recommend wrote the file. And an "optional" subsystem is not actually optional if its failure mode is unbounded — `observer.Start` blocking on an untimed `WaitForCacheSync` turned the one subsystem a code comment promised "a failure here must not stop the console" about into exactly what CrashLoopBackOff'd it. Read a comment's claim against the failure path it does not cover, not just the one it does. And from 2b-iii: **a plan's technical claims need the same verification as its code, and this is not hypothetical caution** — at least three of this phase's own dispatched briefs were wrong on a technical point and had to be corrected mid-execution: Task 5's original ruling on resolving a Pod's tracked trouble (Ruling 14(b)) was fixed at the wrong altitude and had to be re-fixed twice more (Rulings 17, then 20) before it held; Task 6's brief specified bounding an Event dedupe map by `engine.Attribution.Generation`, which never resets for the process's life and would have defeated the dedupe entirely rather than bounding it; and Task 8's brief specified a grouping assertion for a real-install e2e check that fails against *correct* telemetry, for reasons only a real KWOK run exposed. Every one was caught because someone checked the claim against running code or a real cluster before building further on it, not because the brief read confidently. A confident brief is not evidence; treat it the same as a green test suite until verified.
